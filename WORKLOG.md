# บันทึกความคืบหน้า — CRS Score

ไฟล์นี้บันทึกการเปลี่ยนแปลงโค้ด การตัดสินใจ และผลการตรวจสอบที่เกิดขึ้นหลังจากบริบทหลักใน `AGENTS.md` / `CLAUDE.md` เพื่อให้เริ่มงานรอบถัดไปได้ต่อเนื่อง

## 2026-07-15

- เริ่มทำงานต่อในโฟลเดอร์พัฒนา `/Users/nonpatan/Projects/CRS_Score_Antigravity`
- ยืนยันว่าโฟลเดอร์นี้ **ไม่ใช่** ชุดสำหรับอัปโหลด/deploy
- โฟลเดอร์ชุดอัปโหลดอยู่ที่ `/Users/nonpatan/Library/CloudStorage/GoogleDrive-nonpatan@gmail.com/ไดรฟ์ของฉัน/งานวิชาการ/วัดผล/ProjectScore/CRS_Score`
- ตั้งข้อตกลง: ทุกการแก้โค้ดหรือการตัดสินใจสำคัญให้บันทึกต่อท้ายไฟล์นี้ก่อนส่งมอบงาน
- เพิ่มกติกา: ก่อนแก้ไขงานใด ต้องอ่านโค้ดส่วนที่เกี่ยวข้องและจุดที่เรียกใช้งานจริงก่อนเสมอ
- ล็อกแนวทางสมรรถนะหลัก: การเลือก/กำหนดสมรรถนะหลักของ **รายวิชา** ต้องทำที่ `manage.html`
  เท่านั้น โดยเลือกจากรายการกลาง 6 ด้าน; `entry.html` คงหน้าที่กรอกคะแนนตามโครงสร้างที่สร้าง
  ไว้แล้ว ไม่ใช้เป็นจุดกำหนดโครงสร้าง
- แก้ความเข้าใจเรื่ององค์ประกอบ: องค์ประกอบของสมรรถนะหลักมีรายการกลางตายตัวเช่นกัน ไม่ให้ครู
  พิมพ์ชื่อเอง ครูเลือกเฉพาะสมรรถนะและองค์ประกอบที่ต้องการใช้ในรายวิชา/กิจกรรม/กิจวัตร แล้วกำหนด
  คะแนนหรือน้ำหนักขององค์ประกอบนั้นตามบริบท
- อ่านไฟล์ต้นฉบับจริง 3 ช่วงชั้น (ไฟล์ Excel ใน Google Drive) แล้ว: แต่ละไฟล์มี 6 สมรรถนะหลัก
  พร้อมองค์ประกอบมาตรฐาน และ descriptor การประเมินรายองค์ประกอบ 4 ระดับ (4/3/2/1) จึงต้องเก็บ
  องค์ประกอบและ rubric แยกตามช่วงชั้น ไม่ใช่ใช้ชื่อรายการเดียวแบบไม่ระบุช่วงชั้น; ตัวอย่างความต่าง
  คือสมรรถนะพลเมืองมี 3 องค์ประกอบในช่วงชั้น 1 แต่มี 4 องค์ประกอบในช่วงชั้น 2–3
- ยืนยันขอบเขตสมรรถนะหลักในรายวิชา: คงวิธีให้คะแนนเดิมทั้งหมดใน `entry.html` (โครงสร้าง
  unit → indicator → collection → score และครูกำหนดคะแนนเหมือนเดิม) ปรับเฉพาะ `manage.html`
  ให้เลือกชื่อสมรรถนะหลักและชื่อองค์ประกอบจากรายการกลาง ไม่ใช้ rubric 4/3/2/1 มาบังคับกับ
  คะแนนรายวิชา
- ยืนยันการเลือกนักเรียนสำหรับกิจกรรมและกิจวัตร: รองรับทั้งการเพิ่มนักเรียน **ทั้งห้อง** เป็น
  ทางลัด และการเลือก/เพิ่ม/เอาออก **เป็นรายคน** ได้อิสระ เพื่อรองรับกิจกรรมที่มีผู้เข้าร่วมไม่ครบห้อง
  หรือคละกลุ่ม
- ยืนยันวิธีให้คะแนนกิจกรรมและกิจวัตร: ใช้วิธีเดียวกับรายวิชา คือครูเลือกสมรรถนะ/องค์ประกอบ
  กำหนดคะแนนเต็มของครั้งประเมิน และกรอกคะแนนดิบของนักเรียนเอง ไม่บังคับระดับ 4/3/2/1 และไม่
  นำข้อความ rubric จาก Excel มาแสดงหรือใช้คำนวณในโปรแกรม; rubric ให้ครูจัดทำ/พิจารณานอกระบบ
  ไฟล์ Excel ใช้เป็นแหล่งรายการสมรรถนะและองค์ประกอบมาตรฐานเท่านั้น
- ยืนยันว่ากิจกรรม/กิจวัตรไม่ผูกกับชั้นหรือห้องเดียว: รายการหนึ่งรองรับนักเรียนคละหลายชั้น
  หลายห้อง และหลายช่วงชั้นได้ เช่น ละหมาดชาย ม.1–ม.3 และละหมาดหญิง ป.1–ป.6 ร่วมกับ
  ม.1–ม.3; การเลือกสมาชิกต้องรองรับทั้งตัวกรองแบบกลุ่มและปรับเพิ่ม/เอาออกรายคน
- ตรวจโค้ดแล้ว `students` ยังไม่มีฟิลด์เพศ หากต้องการสร้างกลุ่มชาย/หญิงอัตโนมัติ ต้องเพิ่มข้อมูล
  เพศในฐานข้อมูลและหน้า `students.html`/การนำเข้ารายชื่อ หรือเลือกสมาชิกกลุ่มด้วยมือ
- ยืนยันภายหลังว่า **ไม่เพิ่มฟิลด์เพศ**: ครูจะเลือกผู้เข้าร่วมจากรายชื่อนักเรียนเอง สามารถเลือก
  หลายคนคละชั้น/ห้องและบันทึกกลุ่มไว้ใช้ซ้ำได้ ชื่อกลุ่มอาจเป็น “ละหมาดชาย/ละหมาดหญิง” แต่ระบบ
  ไม่กรองเพศอัตโนมัติ
- ลงมือ implement โมดูลสมรรถนะกิจกรรม/กิจวัตรแล้ว: เพิ่ม catalog 6 สมรรถนะ+องค์ประกอบ 3
  ช่วงชั้น, ตารางรายการ/องค์ประกอบ/สมาชิก/ครั้งประเมิน/snapshot รายชื่อ/คะแนนดิบ, RLS แบบ
  owner-or-admin, หน้า `competency-manage.html` และ `competency-entry.html`, และ navigation ทุกหน้า
  (ยังไม่รัน SQL กับ Supabase; ต้องรันเฉพาะบล็อกใหม่ที่ท้าย `schema.sql` ก่อนใช้งานจริง)
- ปรับ `manage.html` ให้เพิ่ม/แก้สมรรถนะหลักของรายวิชาด้วยรายการกลาง และเลือกองค์ประกอบตาม
  ช่วงชั้นของวิชา โดยคงวิธีกรอกคะแนนเดิมทั้งหมด
- ตรวจ syntax ของ `manage.html`, `competency-manage.html`, `competency-entry.html` และ
  `supabase-client.js` ผ่านด้วย Node แล้ว
- ยังไม่เชื่อมคะแนนกิจกรรม/กิจวัตรเข้ากับ `summary.html` เพราะยังไม่ได้ล็อกสูตรหรือรูปแบบการ
  รวม 3 แหล่ง (ต้องไม่ถัวเฉลี่ยแหล่งที่ยังไม่มีข้อมูล)
- หลังทดสอบหน้า deploy พบว่าหน้าใหม่ `competency-manage.html` และ `competency-entry.html`
  ใช้ CSS คนละชุดกับหน้าเดิม ทำให้ความกว้างเป็น 760/860px สี ระยะห่าง หัวข้อ และเมนูไม่ตรงกัน
  จึงปรับทั้งสองหน้าให้ใช้มาตรฐานเดิม: ความกว้าง 560px, สี/ตัวอักษร/การ์ด/ช่องกรอก/ปุ่ม/เมนู
  แบบเดียวกับหน้าอื่น พร้อมเติมเมนูให้ครบและไฮไลต์หน้าปัจจุบันอัตโนมัติ
- ปรับ responsive ทั้งระบบตามที่ผู้ใช้ยืนยัน: เพิ่ม `app-shell.js` เป็นแหล่งเดียวของเมนูที่แสดง
  ในหน้าล็อกอิน 10 หน้า และ `app-shell.css` สำหรับ responsive ร่วมกัน — เมนูเป็นแถบเลื่อน
  แนวนอนพร้อม touch target 44px, โทรศัพท์ (≤600px) ยุบฟอร์ม 2 คอลัมน์/ตารางสรุปเป็นแนวตั้ง
  และ iPad (≥700px) ขยายพื้นที่เนื้อหาเป็น 720px; คง logic และการกรอกข้อมูลเดิมทั้งหมด
- ปรับหน้า `login.html` ให้เป็นหน้าต้อนรับ responsive: ใช้สี teal ร่วมกับสีฟ้าของโลโก้,
  การ์ดโปร่งพร้อมพื้นหลังไล่สี, ช่องกรอก/ปุ่มสูง 50px สำหรับการแตะ, มือถือเรียงแนวตั้งและ
  iPad แสดงแบรนด์กับฟอร์มแบบ 2 คอลัมน์ โดยไม่เปลี่ยน logic Supabase Auth เดิม
- ปรับความสวยงามหน้าที่ล็อกอินทั้งหมดผ่าน `app-shell.css` กลาง: ใช้พื้นหลัง/การ์ดโปร่ง/เงา
  โทนเดียวกับหน้า Login, เพิ่มลำดับชั้นของหัวข้อและ section, ทำช่องกรอกสูง 48px, ปุ่มหลักแบบ
  gradient และปรับกล่องโครงสร้าง/สมาชิก/รายงานให้มีมิติ โดยยังคง responsive และ logic เดิม
- ปรับหน้า `competency-manage.html` ส่วน “องค์ประกอบที่ใช้”: รวบองค์ประกอบที่เลือกไว้ใต้หัวข้อ
  สมรรถนะหลักเดียวกัน แทนการแสดงชื่อสมรรถนะซ้ำทุกบรรทัด โดยคงช่วงชั้น ชื่อองค์ประกอบ คะแนนเต็ม
  และปุ่มลบรายองค์ประกอบไว้เหมือนเดิม
- เพิ่มการแก้ไขคะแนนเต็มขององค์ประกอบที่เลือกใน `competency-manage.html`: เจ้าของรายการหรือ
  admin เปลี่ยนตัวเลขคะแนนเต็มและกด “บันทึก” ได้รายองค์ประกอบ (มีการตรวจว่าต้องมากกว่า 0);
  ผู้มีสิทธิ์ดูอย่างเดียวเห็นคะแนนเต็มเป็นข้อความตามเดิม
- ตรวจไวยากรณ์ JavaScript ของ `competency-manage.html` ด้วย `node --check` แล้วผ่าน
- ปรับตามการใช้งานบนมือถือ: เอาปุ่มบันทึกคะแนนเต็มรายองค์ประกอบออก เหลือช่องคะแนนเต็มและปุ่ม
  ลบในแต่ละบรรทัด แล้วเพิ่มปุ่มเดียว “บันทึกคะแนนเต็มทั้งหมด” ใต้รายการ เพื่อบันทึกเฉพาะค่า
  ที่เปลี่ยนพร้อมกัน
- ตรวจไวยากรณ์ JavaScript หลังปรับการบันทึกแบบชุดด้วย `node --check` แล้วผ่าน
- จัดเมนูกลางใน `app-shell.js` เป็น 3 กลุ่มตามงานเพื่อค้นหาง่ายขึ้นทุกหน้า: “ลงคะแนน”
  (กรอกคะแนน/ลงคะแนนสมรรถนะ/เช็คชื่อ), “กำหนดค่า” (จัดการโครงสร้าง/กำหนดสมรรถนะ/นักเรียน/
  ขึ้นปีใหม่) และ “รายงาน” (สรุปคะแนน/เฝ้าระวัง มส./เรียนซ้ำชั้น); ปรับ `app-shell.css` ให้
  แต่ละกลุ่มมีหัวข้อและกรอบแยก โดยยังเลื่อนแนวนอนได้บนมือถือและ iPad
- ตรวจ `app-shell.js` ด้วย `node --check` และตรวจวงเล็บปีกกาของ `app-shell.css` แล้วผ่าน
- ปรับปุ่มลบใน `competency-manage.html` ให้ใช้ไอคอนถังขยะ `🗑` และ hover สีแดงแบบเดียวกับ
  หน้าจัดการนักเรียน ทั้งการลบองค์ประกอบและเอานักเรียนออกจากรายการ
- ตรวจ JavaScript และวงเล็บปีกกาของ CSS ใน `competency-manage.html` หลังปรับปุ่มแล้วผ่าน
- แก้ช่อง “วันที่ประเมิน” ใน `competency-entry.html` ที่ล้นด้านขวาบน Safari/iPhone: เปลี่ยน
  แถววันที่+ปุ่มสร้างครั้งประเมินเป็น CSS grid ที่ใช้ `minmax(0, 1fr)` และกำหนดความกว้าง
  `#f-date` ไม่ให้เกินการ์ด; หน้าจอโทรศัพท์เรียงวันที่กับปุ่มคนละบรรทัด ส่วน iPad ขึ้นไปยังวาง
  สองคอลัมน์
- ตรวจ JavaScript และวงเล็บปีกกาของ CSS ใน `competency-entry.html` หลังแก้แล้วผ่าน
- ปรับการแสดงฟอร์มคะแนนรายนักเรียนใน `competency-entry.html`: ยังคัดองค์ประกอบตามช่วงชั้นของ
  นักเรียนแต่ละคนเหมือนเดิม (เช่น ม.2/ม.3 ใช้เฉพาะช่วงชั้น 3) แต่รวบหลายองค์ประกอบใต้ชื่อ
  สมรรถนะหลักเดียวกัน จึงไม่แสดงชื่อ “การจัดการตนเอง” ซ้ำทุกบรรทัด
- ตรวจ JavaScript และวงเล็บปีกกาของ CSS ใน `competency-entry.html` หลังจัดกลุ่มแล้วผ่าน
- ยืนยันพฤติกรรมการบันทึกคะแนนกิจกรรม/กิจวัตร: ช่องที่เว้นว่างหมายถึง “ยังไม่ประเมิน” และไม่
  สร้าง/ไม่ทับแถวคะแนนในฐานข้อมูล; หากเคยมีคะแนนแล้วลบตัวเลขให้ว่าง ระบบยังเก็บคะแนนเดิมไว้
  เพื่อไม่ให้ลบข้อมูลโดยไม่ตั้งใจ และใช้ตรวจตามนักเรียนที่ยังไม่มีคะแนนได้
- เปลี่ยนวิธีแก้คะแนนของโมดูลกิจกรรม/กิจวัตรให้ใช้ popup รายรายการตามแบบหน้า `manage.html`:
  `competency-manage.html` แก้คะแนนเต็มขององค์ประกอบผ่านปุ่ม ✎ และบันทึกเฉพาะรายการนั้น;
  `competency-entry.html` แก้คะแนนดิบของนักเรียนแต่ละองค์ประกอบผ่านปุ่ม ✎ และ upsert เฉพาะ
  คะแนนนั้น จึงเอาปุ่มบันทึกคะแนนเต็มทั้งหมด/บันทึกคะแนนทั้งหน้าออก
- ตรวจ JavaScript ของทั้ง `competency-manage.html` และ `competency-entry.html` ด้วย `node --check`
  พร้อมตรวจวงเล็บปีกกาของ CSS และค้นหาการอ้างอิงปุ่มบันทึกแบบเดิมแล้วผ่าน
- แก้ข้อความฟิลด์เจ้าของวิชาใน `manage.html` ให้ตรงกับ RLS จริง: เจ้าของวิชาแก้ไขได้ แต่การลบ
  วิชาสงวนสิทธิ์เฉพาะ admin
- ปรับ workflow ลงคะแนนสมรรถนะจากกิจกรรม/กิจวัตรตามที่ยืนยัน: เอา “วันที่ประเมิน” ออกจาก
  `competency-entry.html` และเปลี่ยนให้แต่ละองค์ประกอบมีครั้งที่ 1, 2, 3... แยกของตัวเอง;
  เลือกครั้งที่แล้วจึงแสดงเฉพาะนักเรียนช่วงชั้นที่ตรงกับองค์ประกอบและแก้คะแนนผ่าน popup รายคน
- เพิ่ม migration ท้าย `schema.sql` ให้ `competency_assessment_sessions` ผูก `target_id` และ
  `attempt_no`; migration แตก session รุ่นเก่าที่เคยครอบหลายองค์ประกอบเป็น session รายองค์ประกอบ
  พร้อมย้าย snapshot/คะแนนเดิมไปด้วย เพื่อไม่ให้คะแนนทดลองสูญหาย และยกเลิกการบังคับ `session_date`
- ตรวจ JavaScript ของ `competency-entry.html` ด้วย `node --check`, ตรวจวงเล็บ CSS และค้นหา
  reference ของช่องวันที่/ปุ่มสร้าง session แบบเดิมแล้วผ่าน; ยังไม่ deploy หน้าใหม่จนกว่าจะรัน
  migration ใน Supabase สำเร็จ เพื่อไม่ให้หน้าเว็บจริงเรียกคอลัมน์ที่ฐานข้อมูลยังไม่มี

## 2026-07-16

- ผู้ใช้ยืนยันว่ารัน migration ก้อนล่าสุดของโมดูลสมรรถนะกิจกรรม/กิจวัตรบน Supabase เรียบร้อยแล้ว
  (`competency_assessment_sessions` ผูก `target_id` และ `attempt_no` พร้อม migration ข้อมูลเดิม)
  จึงไม่มีข้อห้าม deploy ที่เกิดจาก schema ฐานข้อมูลไม่ตรงกับ `competency-entry.html` รุ่นล่าสุดแล้ว
- ล็อกแนวทางหน้าสรุปสมรรถนะจาก 3 แหล่ง: ต้องกำหนดน้ำหนัก “รายวิชา / กิจกรรม / กิจวัตร
  ประจำวัน” แยกอิสระต่อสมรรถนะหลักทั้ง 6 ด้านที่หน้า `competency-manage.html` โดยน้ำหนักของ
  แต่ละด้านรวมกันเป็น 100% แล้วนำไปใช้คำนวณใน `summary.html`
- ผู้ใช้กำหนดให้มีการตั้ง “ระดับการแปลผล” จากช่วงคะแนนด้วย (ช่วงคะแนนนี้แปลผลว่าอะไร)
  และต้องไม่เดาตัวเลขหรือชื่อระดับเอง; ยังต้องยืนยันต่อว่าชุดระดับใช้ร่วมกันทั้ง 6 ด้านหรือแยกต่อด้าน
  รวมถึงวิธีปรับน้ำหนักเมื่อบางแหล่งยังไม่มีคะแนน
- ยืนยันความหมายของระดับการแปลผล: ใช้แปลคะแนนสุดท้ายหลังรวม 3 แหล่งของสมรรถนะแต่ละด้าน
  เช่น คะแนนการจัดการตนเอง 60 แปลผลเป็น “กำลังพัฒนา”; ระดับไม่ได้ใช้แทนคะแนนดิบระหว่างกรอก
  และยังไม่กำหนดชื่อระดับหรือขอบเขตคะแนนตายตัวจนกว่าผู้ใช้จะระบุ
- ยืนยันรูปแบบการตั้งค่า: น้ำหนักรายวิชา/กิจกรรม/กิจวัตรตั้งแยกอิสระต่อสมรรถนะทั้ง 6 ด้าน
  แต่ช่วงคะแนนและชื่อระดับการแปลผลใช้เป็นเกณฑ์กลางชุดเดียวร่วมกันทุกด้าน เช่น ถ้า 60 อยู่ระดับ
  “กำลังพัฒนา” ก็แปลผลเหมือนกันไม่ว่าจะเป็นการจัดการตนเอง การสื่อสาร หรือสมรรถนะด้านอื่น
- ผู้ใช้ส่งเกณฑ์จริงสำหรับการแปลผลสมรรถนะสำคัญ 6 ด้าน (4 ระดับ): 80–100 = “เหนือความคาดหวัง”
  (ระดับดีเยี่ยม), 70–79 = “สามารถ” (ระดับดี), 60–69 = “กำลังพัฒนา” (ระดับพอใช้), และ
  1–59 = “เริ่มต้น” (ระดับปรับปรุง) ใช้เป็นเกณฑ์กลางร่วมกันทั้ง 6 ด้าน; เอกสารไม่ระบุการแปลผล
  คะแนน 0 จึงต้องให้ผู้ใช้ยืนยันก่อนนำกฎไปใช้จริง
- ผู้ใช้ยืนยันว่าไม่ต้องนำข้อความ “ระดับปรับปรุง” ไปแสดงในระบบ สำหรับช่วงล่างให้แสดงชื่อผล
  เพียง “เริ่มต้น” (ส่วนชื่อระดับอื่นยังคงตามเกณฑ์ที่ส่งมา จนกว่าจะมีคำสั่งเปลี่ยนเพิ่มเติม)
- ผู้ใช้ยืนยันเพิ่มเติมว่าไม่ต้องแสดงคำอธิบาย “ระดับพอใช้”, “ระดับดี” และ “ระดับดีเยี่ยม” ด้วย
  ให้ระบบแสดงเฉพาะชื่อผล 4 ระดับหลักเท่านั้น: “เริ่มต้น”, “กำลังพัฒนา”, “สามารถ” และ
  “เหนือความคาดหวัง”
- ล็อก workflow หน้าดูผลสมรรถนะรายนักเรียน: กรองตามปีการศึกษา → ภาคเรียน (แสดงเฉพาะ
  มัธยมศึกษา) → ชั้น → ห้อง → แสดงรายชื่อนักเรียน; เมื่อคลิกนักเรียนให้แสดงสมรรถนะหลักทั้ง
  6 ด้าน โดยแต่ละด้านเป็นคะแนนรวมถ่วงน้ำหนักจากรายวิชา/กิจกรรม/กิจวัตรประจำวัน และแสดงชื่อ
  ผลการแปลตามเกณฑ์กลาง เช่น “เริ่มต้น” หรือ “กำลังพัฒนา” หากคะแนนแหล่งใดไม่ครบให้แสดงว่า
  ข้อมูลยังไม่ครบและไม่สรุปคะแนน/ระดับของด้านนั้น
- ขยายกติกาตัวกรองภาคเรียนของหน้าผลสมรรถนะ: ระดับประถมศึกษาไม่เลือกภาคเรียนและสรุปข้อมูล
  รวมทั้งปีการศึกษา; ระดับมัธยมศึกษาจึงแสดงตัวเลือกภาคเรียน 1/2 และสรุปแยกตามภาคเรียน
- Implement การตั้งค่าและรายงานสมรรถนะรวม 3 แหล่งแล้ว:
  - `schema.sql` เพิ่ม `competency_assessments.term` (null = ทั้งปี), ตาราง
    `competency_source_weights` สำหรับน้ำหนักรายวิชา/กิจกรรม/กิจวัตรแยกต่อสมรรถนะทั้ง 6 ด้าน
    และ `competency_interpretation_levels` สำหรับเกณฑ์แปลผลกลาง พร้อม RLS อ่านได้เมื่อ login และ
    แก้ได้เฉพาะ admin; seed เกณฑ์ 4 ระดับตามเอกสารจริง (1–59 เริ่มต้น, 60–69 กำลังพัฒนา,
    70–79 สามารถ, 80–100 เหนือความคาดหวัง) โดยไม่ใส่คำอธิบายระดับรอง
  - `competency-manage.html` เพิ่มหน้าตั้งน้ำหนักทั้ง 6 ด้าน (บังคับแต่ละแหล่ง > 0 และรวม 100%),
    แก้ช่วงคะแนน/ชื่อระดับกลาง (ตรวจช่วงต่อกัน 1–100 ไม่ซ้อนและไม่เว้น), และเลือกภาคเรียนหรือ
    “ทั้งปี” ตอนสร้างกิจกรรม/กิจวัตร; ค่ากลางแก้ได้เฉพาะ admin ส่วนครูทั่วไปเห็นแบบอ่านอย่างเดียว
  - `supabase-client.js` เพิ่มสูตรกลาง `computeSubjectCompetencySource`,
    `computeAssessmentCompetencySource`, `computeCombinedCompetencyResult`: คะแนนรายวิชารวมตาม
    โครงสร้าง unit→indicator→collection เดิม, กิจกรรม/กิจวัตรรวมคะแนนดิบทุกครั้งเทียบคะแนนเต็มรวม,
    และจะถ่วงน้ำหนัก/แปลผลก็ต่อเมื่อทั้ง 3 แหล่งมีคะแนนครบเท่านั้น
  - `summary.html` เพิ่มโหมด “สมรรถนะรายนักเรียน” โดยคงรายงานวิชาเดิมไว้: เลือกปี→ชั้น→
    (มัธยมเลือกภาคเรียน; ประถมรวมทั้งปี)→ห้อง→คลิกรายชื่อนักเรียน แล้วแสดงครบ 6 ด้าน พร้อมคะแนน
    แต่ละแหล่ง น้ำหนัก คะแนนรวมเต็ม 100 และชื่อผลแปล; ถ้าแหล่งใดขาดจะแสดงจำนวนที่กรอกแล้ว/จำนวน
    ที่ต้องกรอก และไม่สรุปคะแนนหรือระดับด้านนั้น
  - รายการกิจกรรม/กิจวัตร `term=null` ถือเป็น “ทั้งปี”: ประถมรวมรายการทุกภาคเรียนในปีนั้น;
    มัธยมรวมรายการทั้งปีร่วมกับรายการของภาคเรียนที่เลือก ส่วนข้อมูลเก่าทั้งหมดจะเป็นทั้งปีอัตโนมัติ
- ตรวจ JavaScript modules ของ `supabase-client.js`, `competency-manage.html`,
  `competency-entry.html`, `summary.html` และวงเล็บ CSS ผ่าน; ทดสอบสูตรด้วยกรณีคะแนนรายวิชาครบ,
  คะแนนขาด, ถ่วงน้ำหนัก 50/30/20 และการไม่สรุปเมื่อขาดแหล่งใดแหล่งหนึ่งแล้วผ่านทั้งหมด
- **ต้องรัน migration ใหม่ท้าย `schema.sql`** หัวข้อ “น้ำหนักรวมสมรรถนะ 3 แหล่ง + เกณฑ์แปลผลกลาง
  และภาคเรียนของกิจกรรม/กิจวัตร (2026-07-16)” บน Supabase ก่อนใช้/deploy ไฟล์รุ่นนี้; migration
  ครั้งที่ผู้ใช้ยืนยันว่ารันแล้วก่อนหน้านี้เป็นคนละก้อน (target_id/attempt_no)
- ผู้ใช้ยืนยันว่ารัน migration “น้ำหนักรวมสมรรถนะ 3 แหล่ง + เกณฑ์แปลผลกลาง และภาคเรียนของ
  กิจกรรม/กิจวัตร” บน Supabase สำเร็จแล้ว (`Success. No rows returned`) จึงไม่มี schema blocker
  สำหรับ `competency-manage.html` และโหมดสมรรถนะรายนักเรียนใน `summary.html` รุ่นนี้แล้ว
- ยืนยันกรณีกิจกรรม/กิจวัตรที่มีนักเรียนคละประถมและมัธยม: รายการยังระบุภาคเรียนของการประเมิน
  เพียงครั้งเดียว; นักเรียนมัธยมถูกดึงไปสรุปเฉพาะภาคเรียนนั้น แต่นักเรียนประถมสะสมรายการจาก
  ภาคเรียน 1 และ 2 แล้วสรุปรวมทั้งปี รายการที่เลือก “ตลอดปีการศึกษา” ใช้ได้ทั้งปีตามเดิม;
  ปรับข้อความใน `competency-manage.html` จาก “ภาคเรียน” เป็น “ช่วงเวลาที่ประเมิน” พร้อมคำอธิบาย
  ใต้ช่อง เพื่อให้ครูเข้าใจเหตุผลของตัวเลือกนี้
- ปรับถ้อยคำอีกครั้งตามความหมายที่ผู้ใช้ยืนยัน: ช่องดังกล่าวไม่ใช่วันที่/ช่วงที่ครูทำการประเมิน แต่คือ
  “ช่วงเวลาที่นำคะแนนไปสรุปผล” — มัธยมนำคะแนนกิจกรรมไปประมวลผลในภาคเรียนที่เลือก ส่วนประถม
  บันทึกคะแนนไว้ตามปกติแต่ยังไม่สรุป จนกว่าจะนำคะแนนทั้งปีมารวมเมื่อสิ้นปีการศึกษา
- หลังทดลองหน้าเว็บ ผู้ใช้ยืนยันว่าชื่อช่อง “ช่วงเวลาที่นำคะแนนไปสรุปผล” อ่านแล้วสับสน จึงปรับกลับ
  เป็นคำสั้นตรงตัวว่า “ภาคเรียน” และแสดงหมายเหตุใต้ช่องว่า สำหรับนักเรียนประถมระบบจะเก็บคะแนน
  จากแต่ละภาคเรียนไว้รวมประมวลผลเมื่อสิ้นปีการศึกษา
- ล็อก workflow ที่ง่ายกว่าสำหรับกิจกรรม/กิจวัตรข้ามปี: ไม่ใช้ตัวเลือก “ตลอดปีการศึกษา” และไม่ติ๊ก
  หลายภาคเรียนในรายการเดียว ให้สร้างรายการชื่อซ้ำกันได้แต่แยกคนละภาคเรียน เช่น “โฮมรูม” เทอม 1
  และ “โฮมรูม” เทอม 2; หน้า `competency-entry.html` เพิ่มตัวกรองปีการศึกษา+ภาคเรียนก่อนแสดง
  รายการลงคะแนน ส่วน `summary.html` มัธยมดึงเฉพาะรายการของเทอมที่เลือก และประถมรวมรายการ
  เทอม 1+2 ของปีนั้นเมื่อสรุปสิ้นปี
- ผู้ใช้ยืนยันให้ตั้งน้ำหนักรายวิชา/กิจกรรม/กิจวัตรเป็น 0% ได้ในหน้า `competency-manage.html`:
  แหล่งที่ 0% ไม่จำเป็นต้องมีคะแนนของสมรรถนะด้านนั้น และไม่ถือว่าข้อมูลไม่ครบ; ยังบังคับให้
  น้ำหนักรวมต่อสมรรถนะเท่ากับ 100% เสมอ จึงต้องมีอย่างน้อยหนึ่งแหล่งที่ใช้คำนวณ ปรับ validation
  หน้าเว็บ, `computeCombinedCompetencyResult()` และการแสดงผล `summary.html` แล้ว พร้อมเพิ่ม
  migration ท้าย `schema.sql` เพื่อแก้ database constraint เดิมให้รับค่า 0 ได้
- ยืนยันการจัดการข้อมูลไม่ครบ (ปรับตามกติกาน้ำหนัก 0%): ถ้านักเรียนขาดคะแนนจากแหล่งที่มี
  น้ำหนักมากกว่า 0% ห้ามปรับสัดส่วนน้ำหนักจากแหล่งที่มีอยู่และห้ามสรุปคะแนนสุดท้าย ให้แสดง
  “ข้อมูลยังไม่ครบ — ไม่สามารถสรุปคะแนนได้”; แหล่งน้ำหนัก 0% ไม่จำเป็นต้องมีคะแนนและไม่ถือว่า
  ข้อมูลขาด
- ผู้ใช้ยืนยันว่ารัน migration “อนุญาตน้ำหนักแหล่งคะแนนเป็น 0%” บน Supabase สำเร็จแล้ว
  (`Success. No rows returned`) จึงบันทึกน้ำหนัก 0% จากหน้า `competency-manage.html` ได้จริง
- ปรับ UX หน้า `competency-entry.html`: ปุ่ม “ครั้งที่” ที่กำลังเปิดกรอกคะแนนอยู่ แสดงข้อความ
  “กำลังกรอก” และไฮไลต์ด้วยสี teal ขอบหนา และเงา เพื่อให้ครูเห็นทันทีว่ากำลังทำงานกับครั้งใด
- เพิ่มความต่างของสีปุ่มครั้งที่กำลังกรอกตามที่ผู้ใช้ท้วง: ใช้สีเขียวเข้ม `#075442` ตัวอักษรขาว
  พร้อม `!important` กัน CSS กลางทับ ขอบเขียวเข้มกว่า และเงารอบปุ่มที่เด่นขึ้น
- ผู้ใช้ขอไม่ให้แสดงข้อความ “กำลังกรอก” บนปุ่มครั้งที่: คงการไฮไลต์ด้วยสี/ขอบ/เงาไว้ แต่ปุ่ม
  แสดงเพียง “ครั้งที่ 1”, “ครั้งที่ 2” ตามปกติ
- ปรับหน้า `competency-entry.html` ลดความรกของรายการองค์ประกอบ: เพิ่มตัวเลือก “ช่วงชั้นที่ต้องการ
  ลงคะแนน” (ช่วงชั้น 1 ป.1–ป.3 / ช่วงชั้น 2 ป.4–ป.6 / ช่วงชั้น 3 ม.1–ม.6) หลังเลือกกิจกรรม;
  แสดงเฉพาะองค์ประกอบของช่วงชั้นที่เลือกและซ่อน badge ช่วงชั้นซ้ำในแต่ละบรรทัด นักเรียนที่ snapshot
  ตอนเพิ่มครั้งประเมินยังคัดด้วย `competencyStageForGrade()` จึงตรงกับช่วงชั้นที่ครูเลือกเสมอ
- ปรับข้อความตัวเลือกช่วงชั้นที่ 3 ใน `competency-entry.html` จาก “ม.1–ม.6” เป็น “ม.1–ม.3”
  ให้ตรงกับชั้นสูงสุดที่โรงเรียนเปิดสอนจริงในปัจจุบัน
- ปรับลำดับฟอร์มหน้า `competency-entry.html` ให้ตัวเลือก “ช่วงชั้นที่ต้องการลงคะแนน” อยู่ใต้ช่อง
  “กิจกรรม / กิจวัตร” ตาม flow ปีการศึกษา → ภาคเรียน → แหล่งคะแนน → รายการ → ช่วงชั้น
- ปรับตัวเลือกช่วงชั้นใน `competency-entry.html` ให้ dynamic ตามรายการที่เลือก: เริ่มต้น disabled
  จนกว่าจะเลือกกิจกรรม/กิจวัตร แล้วจึงแสดงเฉพาะช่วงชั้นที่มีทั้งนักเรียนผู้เข้าร่วมจริงและองค์ประกอบ
  สมรรถนะของช่วงชั้นนั้น (intersection ของ `competency_assessment_members.student.grade_level` กับ
  `competency_assessment_targets.element.stage`) เพื่อไม่ให้ครูเลือกช่วงชั้นที่ไม่มีนักเรียน
- เปลี่ยนชื่อเมนูนำทาง `summary.html` ทุกหน้าจาก “สรุปคะแนน” เป็น “สรุปผลการเรียน” เพื่อให้ตรงกับ
  เนื้อหาหน้านี้ที่มีทั้งผลคะแนน/เกรดรายวิชาและผลสมรรถนะรายนักเรียน
- แก้จุดสร้างเมนูจริงใน `app-shell.js` จาก “สรุปคะแนน” เป็น “สรุปผลการเรียน” เพิ่มเติม: แถบเมนู
  ที่ผู้ใช้เห็นถูกสร้างจากไฟล์กลางนี้ จึงไม่ได้ใช้ markup เมนูสำรองในแต่ละหน้าโดยตรง
- เพิ่ม query version `app-shell.js?v=20260716-2` ในทุกหน้าที่ใช้เมนูกลาง เพื่อให้ browser ดึง
  JavaScript เมนูฉบับล่าสุดหลังรีเฟรช ไม่ติดสำเนาเดิมใน cache

## 2026-07-17

- ทดสอบระบบเพื่อจัดทำข้อเสนอแนะโดยไม่บันทึก/แก้ข้อมูลจริง: ตรวจ parse หน้า HTML 12 หน้า
  (classic script 11 ชุด, module script 11 ชุด), `app-shell.js` และ `supabase-client.js` ผ่านทั้งหมด
- ทดสอบฟังก์ชันสูตรกลางผ่าน: ขอบเขตเกรด 0–4, การแบ่งช่วงชั้น, ความครบของคะแนนสมรรถนะรายวิชา/
  กิจกรรม, น้ำหนักแหล่ง 0%, การไม่สรุปเมื่อแหล่งที่มีน้ำหนักยังไม่ครบ, การนับขาด/ลา/มาสาย,
  มส. แบบเรียนเพิ่ม/เรียนซ้ำ, ชั่วโมงชดเชย และลำดับ มส. ของวิชาบูรณาการ
- ทดสอบหน้าเว็บจริงแบบ read-only: URL รากไป `login.html`, หน้าที่ต้องล็อกอินส่งกลับพร้อม `next`,
  หน้า login บนมือถือ 390×844 แสดงครบและไม่ล้น, รายงานสมรรถนะ ม.3 แสดงภาคเรียน+ห้อง+รายชื่อ,
  ประถมซ่อนภาคเรียน และกรณีข้อมูลไม่ครบแสดงแยก 3 แหล่งกับน้ำหนักถูกต้อง
- พบข้อเสนอแนะสำคัญจากการทดสอบ:
  1. `login.html` ใช้ค่า `next` จาก query string ไปกับ `location.href` โดยไม่จำกัดเป็นหน้าภายใน
     จึงควรป้องกัน open redirect ก่อนใช้งานกับครูวงกว้าง
  2. รายงานสมรรถนะเลือกนักเรียนจาก `getActiveStudents()` และชั้น/ห้องปัจจุบัน ไม่ใช่ roster ตามปี;
     เมื่อขึ้นปีใหม่ รายงานย้อนหลังอาจแสดงคนผิดรุ่นหรือหาเด็กปีเก่าไม่เจอ
  3. หน้าลงคะแนนสมรรถนะกรอง snapshot นักเรียนด้วย `student.grade_level` ปัจจุบันอีกครั้ง ตอนเปิด
     session เก่า เด็กที่เลื่อนข้ามช่วงชั้นอาจหายจากหน้าคะแนนทั้งที่อยู่ใน snapshot เดิม
  4. คะแนนสมรรถนะรวม 0 คำนวณว่า complete แต่ไม่มีผลแปล เพราะเกณฑ์เริ่มที่ 1 คะแนน; ต้องให้ผู้ใช้
     ยืนยันว่าจะให้ 0 เป็น “เริ่มต้น” หรือแสดงว่าไม่มีเกณฑ์ก่อนแก้ตัวเลขจริง
  5. โฟลเดอร์พัฒนาขาด `logo.png` ทำให้ local แสดงรูปเสีย แม้ชุด deploy มีไฟล์และเว็บจริงแสดงปกติ
  6. หน้า protected ส่วนใหญ่ `throw new Error("redirecting to login")` หลัง redirect ทำให้ console
     มี error ที่ตั้งใจสร้างเอง ควรเปลี่ยนเป็นจบการทำงานแบบไม่โยน error เพื่อลด log รบกวน
- แก้ข้อ 1 แล้วที่ `login.html`: เพิ่ม allowlist สำหรับค่า `next` และตรวจทั้ง origin, path ของ
  แอป และชื่อไฟล์ก่อน redirect; ลิงก์ภายนอก/เส้นทางผิดรูปแบบจะไป `entry.html` แทน ใช้ helper
  เดียวกันทั้งกรณีผู้ใช้มี session อยู่แล้วและหลังล็อกอินสำเร็จ
- ทดสอบ helper redirect ผ่าน: หน้าในระบบพร้อม query stringยังกลับได้ตามเดิม ส่วน URL ภายนอก,
  protocol-relative URL, path นอก `/CRS_Score/` และ `login.html` ถูกส่งกลับ `entry.html` ทั้งหมด

## 2026-07-17 — แก้ข้อเสนอแนะ 2: รายงานย้อนหลังนักเรียนผิดรุ่น/ห้อง

- เพิ่ม migration ท้าย `schema.sql` สำหรับตาราง `student_year_placements`:
  `id` เป็น UUID PK, บังคับ `unique(student_id, year)`, `year`/`grade_level`/`classroom` เป็น
  `not null` และกันข้อความว่าง, มี index สำหรับ `(year, grade_level, classroom)` และ RLS
  (ผู้ล็อกอินอ่านได้, admin เขียนได้) โดยไม่ลบหรือแก้คะแนน/วิชา/นักเรียนเดิม
- ไม่เขียนเดาปีเก่าหรือห้องเก่าจากข้อมูลปัจจุบัน: migration มี SQL นำเข้าข้อมูล **ปีปัจจุบันของ
  นักเรียนที่ยัง active เท่านั้น** ที่ยัง comment ไว้ ผู้ใช้ต้องแทนปีจริงและตรวจ
  `students.grade_level`/`classroom` ให้ครบก่อนรัน เพราะข้อมูลห้องของอดีตอาจถูก rollover
  เขียนทับไปแล้ว
- เพิ่ม `getStudentPlacements(year, studentIds?)` ใน `supabase-client.js` เพื่ออ่านชั้น/ห้องตามปี
  พร้อมข้อมูลนักเรียน โดยไม่กรองคนที่ปัจจุบันจบหรือย้ายออก
- ปรับ `summary.html` ทั้งรายงานวิชาและรายงานสมรรถนะ: รายชื่อ ห้อง ตัวกรองห้อง และบัตรผลลัพธ์
  ใช้ placement ของปีที่เลือก/ปีของวิชาแล้ว; ถ้าบางคนยังไม่มี placement จะแจ้งเตือน แต่ยังไม่ซ่อน
  คะแนนคนนั้นออกจากรายงาน
- ปรับ `students.html`: admin ต้องระบุปีการศึกษาก่อนเพิ่มนักเรียน (ทีละคนหรือ Excel) และหน้าเว็บ
  สร้างทั้ง `students` กับ `student_year_placements`; หากบันทึก placement ไม่สำเร็จ จะลบนักเรียนที่
  เพิ่งสร้างกลับเพื่อไม่ให้ข้อมูลค้างคนละตาราง
- ปรับ `rollover.html`: ตอนเลือกปีต้นทางใช้ placement ของปีนั้นจริง, ใช้ห้องปีต้นทางตอน auto-enroll,
  ตรวจ placement ปีต้นทางก่อนเริ่ม และสร้าง/upsert placement ของปีใหม่ให้เฉพาะนักเรียนที่เลื่อนหรือ
  ซ้ำชั้น (ไม่สร้างให้คนจบ/ย้ายออก) ขณะยังคง `students.grade_level`/`classroom` เป็นค่าปีปัจจุบัน
- ปรับ `AGENTS.md` ให้สถาปัตยกรรมใหม่และลำดับ rollover ไม่ขัดกับโค้ดแล้ว
- ตรวจผ่าน: `node --check supabase-client.js`; parse module scripts ของ `students.html`,
  `summary.html`, `rollover.html`; และ static assertions ว่า schema/RLS/unique constraint กับ
  การเรียกใช้ placement ในทั้งสาม flow ครบ (`placement migration wiring: passed`)
- ยังไม่ได้รัน migration หรือทดสอบเขียนข้อมูลจริงบน Supabase เพื่อไม่แตะข้อมูลนักเรียนจริงก่อนผู้ใช้
  รัน SQL และยืนยันผลตรวจข้อมูลเดิม

## 2026-07-17 — สถานะจริงหลังรัน migration และทดสอบ `student_year_placements`

- ผู้ใช้รัน migration สร้าง `public.student_year_placements` พร้อม RLS policy บน Supabase สำเร็จแล้ว
  (`Success. No rows returned`) และรันคำสั่ง backfill นักเรียน active ของปีการศึกษา `2569` สำเร็จแล้ว
  เช่นกัน โดยใช้ `students.grade_level` และ `students.classroom` ปัจจุบันเป็นข้อมูลตั้งต้นของปีนี้
- ทดสอบหน้าเว็บจริงแบบอ่านอย่างเดียวหลัง migration โดยล็อกอินใน local app แล้ว:
  - `summary.html` โหมด “สมรรถนะรายนักเรียน” เลือกปี 2569 → ม.3 → เทอม 1 → ห้อง 1 แสดงนักเรียน
    20 คน; เลือกรายคนแล้วเห็นผลสมรรถนะครบ 6 ด้าน และกรณีคะแนนจากแหล่งที่ใช้น้ำหนักยังไม่ครบ
    แสดง “ข้อมูลยังไม่ครบ — ไม่สามารถสรุปคะแนนได้” ถูกต้อง
  - `rollover.html` พรีวิวปีต้นทาง 2569 → ปีใหม่ 2570 อ่าน placement ได้จริง: นักเรียน active 21 คน
    (ม.2 จำนวน 1 คน, ม.3 จำนวน 20 คน) และจัดกลุ่ม/เสนอทางเลือกเลื่อนชั้นตามชั้นและห้องของปีต้นทาง
    ถูกต้อง
  - ไม่ได้กด “ตรวจสอบและเริ่มขึ้นปีใหม่”, ไม่ได้บันทึกคะแนน และไม่ได้แก้ไขข้อมูลจริงระหว่างทดสอบ
- ข้อจำกัดของการทดสอบ: ยังไม่มีปีประวัติที่เกิดจากการ rollover จริง จึงยังทดสอบผลลัพธ์ “ดูรายงานปีเก่า
  หลังเลื่อนชั้นแล้ว” แบบ end-to-end ไม่ได้; หลังขึ้นปีจริงครั้งแรก ต้องเปิดรายงานปี 2569 อีกครั้ง
  เพื่อตรวจว่านักเรียนที่เลื่อนชั้น/จบแล้วยังปรากฏตามชั้นและห้องเดิม
- พบจุด UI เล็กน้อย (ยังไม่ได้แก้): เมื่อเลือกวิชาที่ไม่มี `units` หน้า `summary.html` แสดงข้อความว่า
  “วิชานี้ไม่มีหน่วยย่อย...” ถูกต้อง แต่ hint เดิม “กำลังโหลดข้อมูล...” ยังปรากฏร่วมด้วย เพราะ branch
  นี้เขียน `elResult` โดยไม่ได้ซ่อน `elEmptyHint`; ไม่กระทบข้อมูลหรือสูตรคำนวณ
- สถานะ deploy: โค้ด placement อยู่ในโฟลเดอร์พัฒนาเท่านั้น ยังไม่ได้คัดลอกไป Git repository บน Google
  Drive, commit หรือ push. ห้าม deploy จนกว่าผู้ใช้สั่งชัดเจน

### งานค้างสำหรับรอบถัดไป

1. ถ้าผู้ใช้อนุมัติ แก้ UX hint “กำลังโหลดข้อมูล...” ที่ค้างใน `summary.html` สำหรับวิชาที่ไม่มี
   โครงสร้างคะแนน
2. ทดสอบรายงานย้อนหลังจริงอีกครั้งหลัง rollover ปีแรก โดยไม่ให้ placement ปีเก่าหายหรือใช้ชั้น/ห้อง
   ปัจจุบันแทน
3. ข้อเสนอแนะจากการทดสอบเดิมที่ยังไม่ได้เริ่ม: ให้ `competency-entry.html` ใช้ snapshot นักเรียน
   ของ session เก่าโดยไม่กรอง `student.grade_level` ปัจจุบันซ้ำ; ตัดสินกติกาคะแนนสมรรถนะรวม 0;
   นำ `logo.png` เข้าสู่โฟลเดอร์พัฒนา; และเลิก throw error ที่ตั้งใจใช้หลัง redirect ไป login

## 2026-07-17 — แก้ hint โหลดข้อมูลค้างในรายงานวิชา

- แก้ `summary.html` กรณีเลือกวิชาพื้นฐานที่ยังไม่มี `units`: ซ่อน `#empty-hint` ที่ก่อนหน้านี้
  แสดง “กำลังโหลดข้อมูล...” ก่อนแสดงข้อความแนะนำให้ไปตั้งโครงสร้างคะแนน จึงไม่เห็นสองข้อความ
  พร้อมกันอีกต่อไป
- ตรวจผ่าน: parse module script และ static assertion ลำดับการซ่อน hint ก่อน render ข้อความ;
  ทดสอบ branch จากโค้ดจริงด้วยข้อมูล `units=[]` ยืนยันว่า hint ถูกซ่อน, ข้อความกรณีไม่มีหน่วย
  ยังคงแสดง และ dropdown นักเรียนถูก reset ตามเดิม (`empty-units UI branch behavior: passed`)
- เปิดหน้า local เพื่อทดสอบแบบ browser ได้ถึงหน้า login แต่ไม่ทำการล็อกอินหรือแก้ข้อมูลจริง;
  ไม่มีการเขียนข้อมูลไป Supabase

## 2026-07-17 — ทดสอบ rollover และรายงานย้อนหลังแบบ end-to-end

- ผู้ใช้ยืนยันให้ใช้ข้อมูลปัจจุบันสำหรับทดสอบ รวมถึงให้ก๊อปรายวิชาได้ และให้กลับมาตั้งปีที่ใช้งาน
  เป็น 2569 หลังจบการทดสอบ จึงรันผ่านหน้า `rollover.html` ด้วยบัญชี admin จากปี `2569` ไปปีทดสอบ
  `9999` ตามค่าเริ่มต้น: เลื่อนชั้น 1 คน, ทำเครื่องหมายจบ 20 คน, ไม่ซ้ำชั้นและไม่ย้ายออก
- Rollover สำเร็จครบ flow: สร้าง placement ปี 9999, ก๊อปวิชาและโครงสร้าง 24 วิชา, ลงทะเบียน
  นักเรียนให้วิชาใหม่ 20 รายการ และ remap สมาชิกวิชาบูรณาการ; ไม่มีการก๊อปคะแนนหรือเช็คชื่อ
  และตรวจ console error ระหว่างทดสอบไม่พบ error
- ตรวจรายงานย้อนหลังที่ `summary.html` หลัง rollover: เลือกโหมดสมรรถนะรายนักเรียน → ปี 2569 →
  ม.3 → เทอม 1 → ห้อง 1 แล้วยังเห็นรายชื่อนักเรียนเดิม 20 คน แม้คนกลุ่มนี้ถูกทำเครื่องหมายจบ
  แล้วในข้อมูลปัจจุบัน จึงยืนยันว่าใช้ `student_year_placements` ของปีรายงาน ไม่ใช่ชั้น/สถานะปัจจุบัน
- ตั้ง active year กลับเป็น `2569` ผ่านตัวเลือกปีในรายงานแล้ว (ตรวจ UI ของรายงานวิชายืนยันว่าเลือก
  ปี 2569 อยู่) ข้อมูลทดสอบปี 9999 ที่เกิดจาก rollover คงไว้ตามที่ตกลง
- ทดสอบซ้ำกรณีวิชาที่ไม่มี `units` ใน `summary.html` จากหน้าเว็บจริง: แสดงเฉพาะข้อความให้ไปตั้ง
  โครงสร้างคะแนน และไม่เหลือ hint “กำลังโหลดข้อมูล...” ร่วมด้วย
- รายงานวิชาบางวิชาที่เลือกทดสอบไม่มีโครงสร้างคะแนนหรือไม่มี roster จึงไม่เหมาะใช้ยืนยันผลรายวิชา
  รายคนในรอบนี้; ไม่ได้สรุปว่าเป็นบัคของ logic ย้อนหลังโดยไม่มีข้อมูลโครงสร้าง/การลงทะเบียนรองรับ

## 2026-07-17 — ล้างข้อมูลหลังทดสอบ rollover

- ผู้ใช้สั่งให้ลบข้อมูลทดสอบปี 9999 หลังยืนยันผลทดสอบแล้ว จึงสร้างหน้า cleanup ชั่วคราวในโฟลเดอร์
  พัฒนา (ไม่ deploy) ที่บังคับตรวจจำนวนก่อนลบ: วิชาปี 9999 = 24, placement ปี 2569 = 21,
  placement ปี 9999 = 1 และนักเรียนที่เปลี่ยนสถานะ = 21; ค่าที่อ่านได้ตรงครบทุกตัวก่อนเปิดปุ่มลบ
- ดำเนินการสำเร็จ: คืนชั้น/ห้องและสถานะ active ของนักเรียน 21 คนตาม placement ปี 2569, ลบวิชา
  ปี 9999 ทั้ง 24 วิชา (cascade ลบโครงสร้าง, enrollment และสมาชิกวิชาบูรณาการที่ clone มาด้วย),
  ลบ placement ปี 9999 และตั้ง active year กลับเป็น 2569
- ตรวจซ้ำที่ `rollover.html`: dropdown ปีต้นทางเหลือเฉพาะ 2569, รายชื่อนักเรียน active กลับมา 21 คน
  และวิชาต้นทาง 2569 กลับมา 24 วิชา; ลบหน้า cleanup ชั่วคราวออกจากโฟลเดอร์แล้ว

## 2026-07-17 — แก้รายชื่อสมรรถนะของครั้งประเมินย้อนหลัง

- ปรับ `competency-entry.html` ตามที่ผู้ใช้ยืนยัน: รายชื่อใน
  `competency_assessment_session_students` คือ snapshot ของครั้งประเมิน และไม่กรองทิ้งด้วย
  `students.grade_level` ปัจจุบันอีกแล้วเมื่อเปิดครั้งเก่า
- ตัวเลือกช่วงชั้นยังแสดงช่วงชั้นที่มีครั้งประเมินอยู่เสมอ แม้นักเรียนเลื่อนข้ามช่วงชั้นแล้ว;
  จึงกดเปิดครั้งเก่าได้ ไม่ถูกบังตั้งแต่ก่อนถึงรายชื่อ
- สมาชิกกิจกรรมและการสร้างครั้งใหม่อ่านชั้นตาม `student_year_placements` ของปีรายการประเมิน;
  ขณะเปิด snapshot ใช้ placement เพียงแสดงชั้น/ห้องปีนั้น หาก placement ขาดยังคงรายชื่อไว้ครบ
  พร้อมข้อความ “ไม่พบชั้น/ห้อง ปี …” โดยไม่เดาข้อมูลจากชั้นปัจจุบัน
- ตรวจผ่านด้วยการ parse module และจำลองกรณีเด็กเลื่อน ป.6 → ม.1: snapshot ยังมีนักเรียนครบ
  1 คน แสดงชั้น ป.6 ของปีเดิมได้; กรณี placement ขาดก็ไม่ตัดรายชื่อ; และช่วงชั้นของ session
  เก่ายังถูกเลือกได้ (`competency historical snapshot behavior: passed`,
  `competency entry static wiring: passed`)
- ทดสอบหน้าเว็บจริงแบบอ่านอย่างเดียว: ล็อกอินแล้วเลือกปี 2569 → ภาคเรียน 1 → กิจวัตร “โฮมรูม”
  → ช่วงชั้น 3 → เปิด “ครั้งที่ 1”; รายชื่อนักเรียนและคะแนนเดิมแสดงครบ และ console ไม่มี error
  (ยังไม่มีข้อมูลหลัง rollover คงเหลือ จึงยืนยันกรณีข้ามปีจาก scenario จำลอง ไม่ได้เขียนข้อมูล
  ทดสอบกลับเข้าฐานข้อมูล)
- ไม่มีการแก้ schema/RLS/คะแนนจริง และยังไม่ได้ deploy

## 2026-07-17 — ตัดประเด็นคะแนนสมรรถนะรวม 0 ออกจากงานค้าง

- ผู้ใช้ยืนยันว่า `0.0 / 100` ในสรุปรายวิชาเป็นผลปกติและไม่ใช่ปัญหา จึงไม่ต้องเปลี่ยน
  กติกาหรือเกณฑ์แปลผลในรอบนี้
- กรณีสมรรถนะรวม 3 แหล่งที่กรอกครบทุกแหล่งเป็น 0 ยังไม่มีข้อมูลใช้งานจริง และไม่ยกเป็น
  งานค้างจนกว่าจะพบความต้องการจริง

## 2026-07-17 — ลบ console error ที่เกิดตอนเด้งไปหน้า login

- แก้หน้า protected 9 หน้า (`entry`, `attendance`, `summary`, `manage`, `students`,
  `retention`, `rollover`, `competency-manage`, `competency-entry`) ให้หลัง `requireAuth()`
  คืนค่าว่าง หน้าเว็บไม่เริ่มโค้ดต่อและไม่โยน `Error("redirecting to login")` ลง console
- คงพฤติกรรมเดิมครบ: `requireAuth()` ยังเป็นผู้เด้งไป `login.html?next=...`; ไม่แก้
  Supabase Auth, RLS หรือ URL redirect ใด ๆ หน้าที่ `warning.html` ใช้ `return` อย่างถูกต้องอยู่แล้ว
  จึงไม่ต้องแก้
- ตรวจผ่าน: parse module และจำลอง `requireAuth() → null` ครบ 9 หน้าโดย module จบได้ปกติ;
  ตรวจไม่เหลือ error ที่ตั้งใจโยน; `node --check supabase-client.js` ผ่าน
- ทดสอบหน้าเว็บจริงขณะล็อกอินแบบอ่านอย่างเดียว: เปิด `entry.html` แล้วหน้าโหลดและเมนู/ตัวเลือก
  แสดงตามปกติ โดย console ไม่มี error
- ไม่มีการแก้ข้อมูลจริงหรือ deploy

## 2026-07-17 — แก้ชั้น/ห้องในหน้ากำหนดสมรรถนะให้ตรงปีของกิจกรรม

- ปรับ `competency-manage.html`: เมื่อเลือกกิจกรรมหรือกิจวัตร ให้ดึง
  `student_year_placements` ของ `current.year` แทน `getActiveStudents()`/ชั้นปัจจุบัน
  ทั้งในรายชื่อสมาชิกเดิมและตัวกรองชั้น/ห้องสำหรับเพิ่มนักเรียน
- สมาชิกเดิมที่ไม่มี placement ของปีนั้นยังแสดงชื่อครบ พร้อมข้อความ “ไม่พบชั้น/ห้อง ปี …”;
  ไม่ใช้ชั้นปัจจุบันเดาแทน และไม่แก้สมาชิก/snapshot/คะแนนที่บันทึกไว้
- การย้อนเปิดกิจกรรมปีเก่าจึงเห็นชั้น/ห้องของปีนั้น แม้นักเรียนปัจจุบันเลื่อนชั้น จบ หรือย้ายออก;
  การเพิ่มทั้งห้อง/เพิ่มรายคนก็ใช้รายชื่อของปีรายการนั้น
- ตรวจผ่าน: parse module และ scenario เด็ก ป.6 ที่ปัจจุบันเป็น ม.1 (แสดง ป.6 ตาม placement),
  ยืนยันกรณี placement ขาดยังไม่ซ่อนชื่อ และ picker ยังคงรวมคนจบในปีเก่า
  (`competency manage historical placement behavior: passed`,
  `competency manage year wiring: passed`)
- ทดสอบหน้าเว็บจริงแบบอ่านอย่างเดียว: เปิดกิจวัตร “โฮมรูม” ปี 2569 แล้วสมาชิก 21 คนและ
  ตัวกรองแสดง ม.2/ม.3 ห้อง 1 ตาม placement ปี 2569; console ไม่มี error
- ไม่มีการแก้ schema/RLS/ข้อมูลจริง และยังไม่ได้ deploy

## 2026-07-17 — ตัดสินกติกา: คะแนนสมรรถนะรวม = 0 แปลผลเป็น "เริ่มต้น"

> ⚠ **ยกเลิกแล้ว — อย่าลงมือทำตามแผนในหัวข้อนี้** ผู้ใช้ทบทวนใหม่ภายหลังแล้วตัดสินว่าไม่แก้
> (ดูบันทึก "ยกเลิกการแก้เกณฑ์ 0" ท้ายไฟล์ วันเดียวกัน)

- **ผู้ใช้ยืนยันแล้ว**: คะแนนสมรรถนะรวม `0` ให้แปลผลเป็น **"เริ่มต้น"** โดยขยายช่วงล่างของเกณฑ์
  กลางจาก `1–59` เป็น `0–59` (ปิดข้อค้างข้อ 4 ของรอบทดสอบ 2026-07-17 ที่ระบุว่าเอกสารจริงของ
  โรงเรียนไม่ได้ระบุการแปลผลคะแนน 0)
- เหตุผลที่ใช้ตัดสิน: ระบบแยก "เว้นว่าง = ยังไม่ประเมิน" (ไม่สร้างแถวคะแนน → ไปเข้าเงื่อนไข
  `complete:false` "ข้อมูลยังไม่ครบ") ออกจาก "0 = ประเมินแล้วได้ 0" ไว้แล้ว จึงไม่จำเป็นต้องกัน 0
  ไว้เป็นกรณีพิเศษ — และเดิมเด็กที่ได้ 1 มีระดับ แต่เด็กที่ได้ 0 ซึ่งต่ำกว่ากลับไม่มีระดับ เป็นผลกลับหัว
- พฤติกรรมเดิมก่อนแก้ (ตรวจแล้ว): `computeCombinedCompetencyResult()` คืน
  `complete:true, score:0, level:null, reason:"คะแนนไม่อยู่ในช่วงเกณฑ์แปลผลที่กำหนด"` →
  `summary.html` แสดง "0.0 / 100" พร้อมข้อความเตือนแทนชื่อระดับ (ไม่ crash แต่ครูอธิบายผลไม่ได้)

### จุดที่ต้องแก้ (ยังไม่ได้ลงมือ)

1. `schema.sql:1102` seed `'beginning'` เปลี่ยน `min_score` จาก `1` → `0`
2. **⚠ ต้องเพิ่ม migration `update` ต่างหาก** — seed ใช้ `on conflict (code) do nothing` และแถว
   `beginning` มีอยู่ในฐานข้อมูลจริงแล้ว **การแก้ seed เฉยๆ จะไม่มีผลกับ DB ที่รันไปแล้ว**:
   `update competency_interpretation_levels set min_score = 0 where code = 'beginning';`
3. `competency-manage.html:313` validation `rows[0].min_score!==1` → `!==0`
4. `competency-manage.html:127` ข้อความ "ช่วงต้องต่อกันตั้งแต่ 1–100" → "0–100"

**ไม่ต้องแก้**: `schema.sql:1097` constraint เขียน `min_score >= 0` อยู่แล้ว รองรับ 0 ได้ตั้งแต่ต้น ·
`supabase-client.js:167` ใช้ `find(l => score >= min && score <= max)` จะรับ 0 ได้เองทันทีที่ข้อมูล
เกณฑ์เปลี่ยน

## 2026-07-17 — ตรวจทานงาน placement รอบข้ามปี (อ่านอย่างเดียว ไม่แก้โค้ด)

ตรวจสถานะหลังงาน `student_year_placements` ยืนยันว่าปิดครบแล้ว:

- `competency-entry.html` ✅ — `availableStagesForAssessment()` มี `sessionStages` ทำให้ครั้งประเมิน
  เดิมเปิดได้เสมอแม้เด็กเลื่อนข้ามช่วงชั้น · การเปิด snapshot ไม่กรองใครทิ้งแล้ว (`applyYearPlacements`
  ใส่ชั้น/ห้องของปีนั้นให้เฉยๆ) · การสร้าง session ใหม่ยังกรองด้วย stage ซึ่งถูกต้อง เพราะอ่านชั้น
  จาก placement ของปีรายการ ไม่ใช่ `students.grade_level` ปัจจุบัน
- `summary.html` ✅ — picker สมรรถนะใช้ `getStudentPlacements(year)` แทน `getActiveStudents()` แล้ว
- `students.html` ✅ — `insertStudentsWithPlacements()` ถูกเรียกจากทั้ง 2 ทาง (ทีละคน + วางจาก Excel)
  พร้อม rollback ลบนักเรียนคืนถ้าเขียน placement ไม่สำเร็จ
- `rollover.html` ✅ — เขียน placement + ทดสอบจริงด้วยปี 9999 และล้างข้อมูลคืนแล้ว

### ข้อค้างที่พบเพิ่ม (ยังไม่ได้แก้)

- **`competency-manage.html` เป็นหน้าเดียวที่ยังไม่ได้ปรับ** — บรรทัด 307 (รายชื่อสมาชิก) และ 310
  (ตัวเลือกเพิ่มนักเรียน) ยังแสดง `student.grade_level`/`classroom` **ปัจจุบัน** → เปิดกิจกรรมปีเก่า
  จะเห็นชั้นของปีปัจจุบัน ไม่ตรงกับ `competency-entry.html` ที่แสดงของปีนั้นถูกแล้ว
  **ความรุนแรงต่ำ**: หน้านี้ไม่มี `competencyStageForGrade` เลย (0 จุด) จึงไม่มีการกรองใครทิ้ง
  ไม่มีเด็กหาย เป็นเพียงป้ายชั้นแสดงผิดปี
- **ยังไม่ deploy — เป็นการตัดสินใจของผู้ใช้ ไม่ใช่งานค้าง**: จะแก้ทุกอย่างให้เสร็จก่อนแล้ว deploy
  ครั้งเดียว (deploy ครึ่งๆ กลางๆ จะทำให้ครูเจอเกณฑ์ 0 ที่ยังไม่แก้ แล้วต้อง deploy ซ้ำ)
  สถานะจริง 3 ชั้น — ตรวจแล้ว 2026-07-17 โดยดึงไฟล์จาก `https://nonpatan.github.io/CRS_Score/`
  มาตรวจตรงๆ:
  - ฐานข้อมูล: ✅ ใหม่แล้ว (ตาราง placement มีจริง 21 แถวปี 2569)
  - โฟลเดอร์พัฒนา: ✅ ใหม่แล้ว
  - เว็บที่ครูใช้จริง: ❌ ยังเก่า — `supabase-client.js`/`students.html`/`competency-entry.html`/
    `summary.html` ไม่มีโค้ด placement เลยสักบรรทัด
  **เว็บจริงตอนนี้ไม่มีปัญหาและใช้งานได้ปกติ** เพราะตารางใหม่ไม่กระทบโค้ดเก่า และยังไม่มีเด็กคนไหน
  ถูกเลื่อนชั้นจริง (การทดสอบ rollover ใช้ปี 9999 และล้างคืนหมดแล้ว) บั๊กข้ามช่วงชั้นจึงยังไม่ออกอาการ
  > ⚠ **ระหว่างที่ยังไม่ deploy ห้ามทำ 2 อย่างบนเว็บจริง**: (1) เพิ่มนักเรียนใหม่ — จะไม่มี placement
  > เขียนตามไปด้วย (แก้ได้ภายหลังด้วยการ insert ย้อนหลัง ไม่ถึงกับหายถาวร) (2) **กดขึ้นปีใหม่** —
  > `rollover.html` ที่ deploy อยู่ยังไม่เขียน placement จะทับชั้น/ห้องเดิมทิ้งโดยไม่จด **กู้ไม่ได้**
- `student_year_placements` ใช้ `id uuid` + `unique (student_id, year)` ตามธรรมเนียมเดิมของไฟล์
  (เคยคุยเรื่องเปลี่ยนเป็น composite PK `(student_id, year)` แต่**ผู้ใช้ตัดสินแล้วว่าไม่ต้องเปลี่ยน**)

## 2026-07-17 — sync CLAUDE.md ให้ตรงกับ AGENTS.md (เอกสารแตกกัน)

- พบว่า `AGENTS.md` ถูกอัปเดตเรื่อง `student_year_placements` แล้ว (ตารางมีจริง + ขั้นตอน rollover
  ใหม่) แต่ `CLAUDE.md` ยังเป็นข้อความเก่าที่บอกว่า "ยังไม่มีตาราง student_year_placements ...
  โมเดลเลื่อนชั้นแบบทับค่าเพียงพอแล้ว" — สองไฟล์นี้ต้องเป็นกระจกเงากัน (ต่างแค่บรรทัดหัวเรื่อง)
  ถ้าปล่อยไว้ agent ที่อ่าน CLAUDE.md จะเชื่อว่าตารางไม่มีอยู่ ทั้งที่ใช้งานบนเว็บจริงแล้ว
- แก้โดยคัดเนื้อหา `AGENTS.md` (ฉบับอัปเดต) ทับ `CLAUDE.md` ทั้งไฟล์ โดยคงบรรทัดหัวเรื่อง
  `# CLAUDE.md — ...` ไว้ ตรวจแล้ว diff เหลือต่างแค่บรรทัดแรกบรรทัดเดียว
- **บทเรียน**: ใครแก้ AGENTS.md หรือ CLAUDE.md ต้องแก้อีกไฟล์ให้ตรงกันทุกครั้ง
- หมายเหตุ: `CLAUDE.md` ในชุด deploy (repo GitHub) ยังเป็นฉบับเก่า — รวมไปกับการ push รอบถัดไป
  ไม่ต้อง push แยกเพราะไม่กระทบการทำงานของเว็บ (เป็นไฟล์เอกสาร)

## 2026-07-17 — ยกเลิกการแก้เกณฑ์ 0 (ทบทวนการตัดสินเดิม)

- **ผู้ใช้ทบทวนแล้วตัดสินใหม่: ไม่แก้** — ยกเลิกแผน 4 จุดในหัวข้อ "ตัดสินกติกา: คะแนนสมรรถนะรวม
  = 0" ด้านบน (ทั้ง seed, SQL update, validation และข้อความใน competency-manage.html)
- เหตุผล: ตรวจข้อมูลจริงบนเว็บแล้ว ไม่มีนักเรียนคนไหนมีคะแนนสมรรถนะรวม = 0 เคสนี้จึงยังไม่เคย
  ปรากฏจริง และการจะเกิดได้ต้องมีครูกรอกคะแนน 0 จริงครบทุกแหล่งที่มีน้ำหนัก (เว้นว่างไม่นับ เพราะ
  เข้าเงื่อนไข "ข้อมูลยังไม่ครบ" แยกไปแล้ว) — เป็น edge case ที่ยอมรับได้ ถ้าเกิดจริงจะเห็นข้อความ
  "คะแนนไม่อยู่ในช่วงเกณฑ์แปลผลที่กำหนด" ซึ่งไม่ใช่ข้อมูลผิด แค่ไม่มีชื่อระดับ ค่อยกลับมาแก้ตอนนั้นได้
- เกณฑ์กลางคงเดิม: 1–59 เริ่มต้น · 60–69 กำลังพัฒนา · 70–79 สามารถ · 80–100 เหนือความคาดหวัง
- งานค้างของระบบจึงเหลือรายการเดียว: `competency-manage.html` แสดงชั้น/ห้องของปีกิจกรรม
  (cosmetic — รายละเอียดในหัวข้อ "ข้อค้างที่พบเพิ่ม")

## 2026-07-17 — ปุ่มแก้ไข/ลบรายการกำหนดสมรรถนะ และสิทธิ์ Admin

- ผู้ใช้ยืนยันให้ทำหน้า `competency-manage.html` เฉพาะ Admin: ฝั่งหน้าเว็บปฏิเสธบัญชีที่ไม่ใช่
  admin ก่อนโหลดข้อมูลหรือเครื่องมือจัดการ และพากลับไปหน้ากรอกคะแนนได้
- เพิ่มปุ่ม “แก้ไข” และ “ลบ” ในทุกรายการที่กำหนดไว้:
  - แก้ไขชื่อ/ปี/ประเภท/ภาคเรียนได้เมื่อยังไม่มีครั้งประเมิน; เมื่อมีครั้งแล้วล็อกปี/ประเภท/
    ภาคเรียนไว้ เหลือแก้ได้เฉพาะชื่อ เพื่อไม่ให้ snapshot และผลสรุปในอดีตคลาดเคลื่อน
  - ก่อนลบ ระบบนับองค์ประกอบ สมาชิก ครั้งประเมิน snapshot และคะแนนที่เกี่ยวข้อง; หากมีครั้ง
    ประเมินหรือคะแนน ต้องพิมพ์ชื่อรายการตรงกันก่อนจึงลบได้ (การลบเป็น cascade ตาม schema)
- ปรับ `schema.sql` ทั้ง policy หลักและ migration ท้ายไฟล์: เฉพาะ Admin สร้าง/แก้/ลบ
  `competency_assessments` และเขียน `competency_assessment_targets`/`members` ได้;
  **ไม่แตะ** policy ของ sessions/snapshot/scores จึงคงให้ครูเจ้าของกิจกรรมสร้างครั้งประเมินและ
  กรอกคะแนนใน `competency-entry.html` ได้เหมือนเดิม
- ตรวจผ่าน: parse module และ static assertions ของ Admin guard, ปุ่มแก้ไข/ลบ, การยืนยันลบ,
  และ RLS policy (`competency management access and action wiring: passed`)
- การทดสอบบนฐานข้อมูลจริงและการรัน RLS migration **รอการล็อกอิน Supabase Dashboard**; เปิดหน้า
  SQL Editor ของ project ไว้แล้วแต่ session Dashboard ไม่มีอยู่ จึงยังไม่ได้เปลี่ยน policy จริง
  หรือสร้าง/ลบรายการทดสอบใด ๆ

## 2026-07-17 — รัน RLS และทดสอบหน้า Admin จริง

- ผู้ใช้รัน RLS migration จำกัดการจัดการรายการสมรรถนะบน Supabase สำเร็จแล้ว
  (`Success. No rows returned`) — ไม่มีการเปลี่ยนหรือลบข้อมูลเดิมจาก SQL ก้อนนี้
- ทดสอบผ่าน `competency-manage.html` ขณะล็อกอิน Admin บนข้อมูลจริง:
  1. สร้างรายการชั่วคราว “ทดสอบแก้ไขลบ Codex 2569” (กิจกรรม, ปี 2569, เทอม 1)
  2. แก้เป็น “ทดสอบแก้ไขลบ Codex สำเร็จ”, เปลี่ยนประเภทเป็นกิจวัตร และเทอม 2 แล้วหน้า list
     แสดงค่าที่แก้ทันที
  3. กดลบและยืนยัน; รายการทดสอบหายจาก list แล้ว — ไม่มีองค์ประกอบ สมาชิก ครั้งประเมิน หรือ
     คะแนนอยู่ใต้รายการนั้น จึงไม่มีข้อมูลจริงถูกลบ
- เปิด “โฮมรูม” เทอม 1 ซึ่งมีครั้งประเมินเดิม: modal แก้ไขล็อกปีการศึกษา/ประเภท/ภาคเรียนครบ
  และแสดงข้อความเตือน เหลือให้แก้ได้เฉพาะชื่อ; ปิด modal โดยไม่บันทึก
- Console หน้าเว็บไม่มี error ระหว่างทดสอบ

## 2026-07-17 — ปรับปุ่มจัดการรายการสมรรถนะเป็นไอคอน

- ตามที่ผู้ใช้ขอ เปลี่ยนปุ่มข้อความ “แก้ไข”/“ลบ” ใน list รายการของ
  `competency-manage.html` เป็นไอคอน ✎/🗑 ให้เหมือนปุ่มจัดการองค์ประกอบและสมาชิกในหน้าเดียวกัน
  โดยคง `title` และ `aria-label` ที่มีชื่อรายการไว้สำหรับ tooltip และการเข้าถึง

## 2026-07-17 — ปรับสิทธิ์รายการสมรรถนะเป็นเจ้าของหรือ Admin

- ผู้ใช้ทบทวนใหม่: หน้า `competency-manage.html` ให้ครูทุกคนสร้างรายการกิจกรรม/กิจวัตรเองได้;
  คงเฉพาะน้ำหนักสมรรถนะ 3 แหล่งและเกณฑ์แปลผลกลางที่ Admin เท่านั้นแก้ได้
- ยกเลิก Admin guard ทั้งหน้าที่เพิ่งเพิ่มไว้ แล้วใช้ `owner_id` ที่มีอยู่แล้วเป็นเจ้าของอัตโนมัติ
  เมื่อครูสร้างรายการ; ใน list แสดง “ผู้กำหนด: คุณ/ชื่อครู” จากตาราง `profiles`
- ปุ่ม ✎/🗑 และการแก้องค์ประกอบ/สมาชิกปรากฏเฉพาะเจ้าของรายการหรือ Admin; ครูคนอื่นยังเปิดดู
  รายการได้ แต่รายละเอียดเป็นอ่านอย่างเดียว
- ปรับ policy หลักและ migration ท้าย `schema.sql` กลับเป็น owner-or-admin สำหรับ
  `competency_assessments`, targets และ members; policy ของน้ำหนัก/เกณฑ์กลางยังเป็น admin-only
  และ policy sessions/scores ยัง owner-or-admin ตามเดิม
- ตรวจผ่าน: parse module และ static assertions ของ owner UI/RLS
  (`owner-based competency management wiring: passed`)
- **ต้องรัน migration ล่าสุดท้าย `schema.sql` บน Supabase อีกครั้ง** เพราะฐานข้อมูลจริงเพิ่งถูก
  ตั้งเป็น admin-only จาก migration ก่อนหน้า; ก้อนล่าสุดเพียงเปลี่ยน policy ไม่แตะข้อมูลรายการหรือคะแนน

## 2026-07-17 — รัน RLS เจ้าของหรือ Admin และทดสอบหน้า Admin

- ผู้ใช้รัน RLS migration owner-or-admin บน Supabase สำเร็จแล้ว (`Success. No rows returned`)
  เป็นการ drop/create policy เท่านั้น ไม่แตะรายการ กิจกรรม สมาชิก ครั้งประเมิน หรือคะแนนเดิม
- ทดสอบหน้า `competency-manage.html` บัญชี Admin กับฐานข้อมูลจริง: รายการเดิมทั้ง 3 รายการ
  แสดง “ผู้กำหนด: คุณ” และไอคอน ✎/🗑 ครบ; น้ำหนัก/เกณฑ์กลางยังแก้ได้ตามสิทธิ์ Admin และ
  console ไม่มี error
- การกันครูคนอื่น: UI ใช้ `canManageAssessment()` (owner หรือ Admin เท่านั้น) และ RLS ใช้
  `can_edit_competency_assessment()` กฎเดียวกันกับฐานข้อมูล; ยังไม่มีบัญชีครูแยกใน session นี้ให้
  ล็อกอินทดสอบแบบ end-to-end เพิ่ม แต่ policy ที่รันจริงและ static wiring ตรวจครบแล้ว

## 2026-07-17 — ระบุผู้สร้างที่หน้าเข้าสู่ระบบ

- เพิ่มข้อความ “ผู้สร้างระบบ: ผอ.อานนท์ ปัญจเมธีกุล” ใต้คำแนะนำติดต่อผู้ดูแลระบบใน `login.html`
  ด้วยขนาดตัวอักษรรอง เพื่อไม่รบกวนฟอร์มเข้าสู่ระบบ

## 2026-07-18 — ยืนยัน deploy แล้ว (ล้างคำเตือน “ยังไม่ deploy” ที่ค้างในบันทึกเก่า)

- ผู้ใช้แจ้งว่าได้ push/deploy งานทั้งหมดขึ้นเว็บจริงแล้ว แต่ WORKLOG ยังไม่ได้อัปเดต จึงยังค้าง
  ข้อความ “ยังไม่ deploy” หลายจุด
- ตรวจยืนยันของจริง โดยดึงไฟล์จาก `https://nonpatan.github.io/CRS_Score/` มาเทียบกับโฟลเดอร์
  พัฒนา (md5 checksum): `supabase-client.js`, `login.html`, `summary.html`, `students.html`,
  `competency-manage.html`, `competency-entry.html`, `rollover.html`, `app-shell.js`
  **ตรงกันเป๊ะทุกไฟล์** — เว็บ live มีโค้ด `student_year_placements`/`getStudentPlacements` และ
  งานล่าสุดสุด (ข้อความ “ผู้สร้างระบบ” ที่ `login.html`) ครบแล้ว
- **สรุปสถานะจริง**: โฟลเดอร์พัฒนา = เว็บ live = ฐานข้อมูล Supabase (migration รันครบแล้ว) —
  ตรงกันทั้ง 3 ชั้น ถือว่า **deploy สมบูรณ์แล้ว**
- **คำเตือนที่หมดอายุ (ไม่บังคับใช้แล้ว)**: ข้อความในบันทึก 2026-07-17 (“ระหว่างที่ยังไม่ deploy
  ห้ามเพิ่มนักเรียนใหม่ / ห้ามกดขึ้นปีใหม่บนเว็บจริง”) — ตอนนี้เว็บจริงมีโค้ด placement ครบแล้ว
  ทั้งการเพิ่มนักเรียน (`students.html` เขียน placement ตามด้วย) และ rollover (`rollover.html`
  เขียน/upsert placement ปีใหม่) ทำงานถูกต้องบน live แล้ว จึง **ปลอดภัยทั้งสองอย่าง**
- ยังไม่มีการแก้โค้ด/schema/ข้อมูลจริงในรอบนี้ — เป็นการตรวจสอบสถานะและอัปเดตเอกสารเท่านั้น

## 2026-07-18 — ลบนักเรียน: แสดงรายละเอียดข้อมูลที่ผูกอยู่ + ปุ่มลบทั้งหมด (`students.html`)

- **ปัญหาที่ผู้ใช้เจอ**: ลบนักเรียนทดสอบ (“ทดสอบ ทดสอบโปรแกรม” เลขที่ 0000 ม.3/1) ไม่ได้ เพราะ
  guard เดิมบล็อกเมื่อมีคะแนน/เช็คชื่อ/ร. แต่บอกแค่ยอดรวม ไม่บอกว่าติดวิชาไหน จึงตามไปลบไม่ถูก
- **ตรวจ schema ก่อนแก้**: ทุก FK ที่ผูก `student_id` เป็น `ON DELETE CASCADE` ทั้งหมด — `scores`,
  `attendance_records`, `remarks`, `enrollments`, `makeup_hours`, `competency_assessment_members`,
  `competency_assessment_session_students`, `competency_assessment_scores`, `student_year_placements`
  → ฐานข้อมูลลบได้เสมอ และลบข้อมูลลูกตามให้ guard เดิมเป็นแค่ **soft-block ฝั่งหน้าเว็บ** กันลบพลาด
- **ผู้ใช้เลือกแนวทาง** (ถามผ่าน AskUserQuestion): “แสดงละเอียด + ปุ่มลบทั้งหมด”
- **สิ่งที่ทำใน `students.html`**:
  - เขียน `deleteStudent()` ใหม่ + helper `gatherStudentLinks()` (ดึงคะแนน/เช็คชื่อ/ร./ชดเชย/
    สมรรถนะ/ลงทะเบียน/placement พร้อมชื่อวิชา-กิจกรรม ผ่าน embedded select ของ Supabase),
    `tallyBySubject()`, `subjLabel()`, `linkSectionHtml()`
  - ถ้านักเรียน **ไม่มี**คะแนน/เช็คชื่อ/ร./ชดเชย/สมรรถนะเลย → ลบได้เลยด้วย `confirm()` สั้น ๆ แบบเดิม
  - ถ้ามีข้อมูลผูกอยู่ → เปิด **modal** โชว์รายละเอียดว่าติดวิชา/กิจกรรมไหนบ้าง (นับเป็นรายวิชา:
    “X ช่อง / X รายการ / X วิชา”) + กล่องเตือนกู้ไม่ได้ + ช่อง **พิมพ์ชื่อนักเรียนให้ตรง**เพื่อ
    ปลดล็อกปุ่ม “ลบนักเรียน + ข้อมูลทั้งหมด” (ลบผ่าน cascade ทีเดียว)
  - เพิ่ม CSS ของ modal + markup `#del-modal` (event listener ลงทะเบียนครั้งเดียวตอน init,
    modal body สร้างใหม่ทุกครั้งที่เปิด ไม่มี listener สะสม)
- **ผลพลอยได้**: ปิดช่องโหว่เดิมที่ guard ไม่เคยเช็ค `makeup_hours` และตารางสมรรถนะ (เมื่อก่อน
  นักเรียนที่มีแต่ข้อมูลสองกลุ่มนี้จะถูกลบเงียบ ๆ ผ่าน confirm สั้น) ตอนนี้เข้าเงื่อนไข modal ด้วย
- **ตรวจผ่าน**: `node --check` module script ผ่าน · ตรวจ id ที่ JS อ้างถึงมีครบใน markup
  (`del-confirm-name` สร้าง dynamic ถูกต้อง) · ตรวจ path ของ embedded select ทุกเส้นตรงกับ FK/ชื่อ
  ตารางใน `schema.sql` และแต่ละชั้นมี FK เดียว (ไม่กำกวม) · ทดสอบ browser เต็มรูปแบบทำไม่ได้เพราะ
  ต้องล็อกอิน Supabase (session ไม่ได้อยู่ฝั่ง agent) — ต้องให้ผู้ใช้ทดสอบบน session ที่ล็อกอินเอง
- **ยังไม่ได้ deploy** — โค้ดอยู่ในโฟลเดอร์พัฒนาเท่านั้น ต้อง push ขึ้น GitHub Pages ก่อนถึงจะใช้
  บนเว็บที่ครูใช้จริงได้ (แก้ `students.html`, `CLAUDE.md`, `AGENTS.md`, `WORKLOG.md`)
- **หมายเหตุ UX**: กล่องเตือนใน modal แนะนำว่า ถ้าอยากเก็บคะแนนไว้แต่ไม่ให้อยู่ในรายชื่อ active
  ให้ใช้ “จบ/ย้ายออก” ที่ rollover แทน — แต่ยังไม่มีปุ่มตั้ง graduated/left_school ตรงในหน้านี้
  (ถ้าผู้ใช้อยากได้ค่อยเพิ่มรอบถัดไป)

## 2026-07-18 — ทดสอบจริงและยืนยันสิทธิ์เข้าหน้า `students.html`

- **ทดสอบ modal ลบนักเรียนบนข้อมูลจริงสำเร็จ**: รันเซิร์ฟเวอร์ static ท้องถิ่น (`python3 -m
  http.server 8787`) เสิร์ฟโฟลเดอร์พัฒนา (จำเป็นเพราะ ES module ผ่าน `file://` ตรง ๆ ใช้ไม่ได้)
  ผู้ใช้ล็อกอินด้วยบัญชี admin จริงที่ `http://127.0.0.1:8787/login.html` แล้วเปิด
  `students.html` → กดลบ “ทดสอบ ทดสอบโปรแกรม” เลขที่ 0000 ม.3/1 (นักเรียนทดสอบที่สร้างผิดพลาด
  ไว้ก่อนหน้านี้ มีคะแนน/เช็คชื่อ/ลงทะเบียนผูกอยู่จริง) — modal แสดงรายละเอียดตามที่ออกแบบไว้
  ผู้ใช้พิมพ์ชื่อยืนยันแล้วกดลบ **สำเร็จ** ลบนักเรียนคนนี้และข้อมูลที่ผูกอยู่ทั้งหมดออกจากฐานข้อมูล
  จริงเรียบร้อยแล้ว (ยืนยันจากผู้ใช้โดยตรง — เป็นการลบถาวรตามที่ตั้งใจ ไม่ใช่อุบัติเหตุ)
- **ยืนยันสิทธิ์เข้าหน้า `students.html`**: ผู้ใช้ถามว่าใครเข้าหน้านี้ได้บ้าง ย้ำว่าต้องเป็น
  admin เท่านั้นที่ “ใช้ได้” — ตรวจโค้ด+RLS จริงแล้วพบว่าสถานะปัจจุบันคือ: **ครูทั่วไปเข้าหน้าได้
  และดูรายชื่อนักเรียนทั้งโรงเรียนได้ (read-only)** แต่ **เขียน/แก้/ลบทำไม่ได้เลย** ทั้งฝั่ง UI
  (ปุ่มเพิ่ม/แก้/ลบไม่แสดงถ้าไม่ใช่ admin — รวมถึงปุ่มถังขยะที่เปิด modal ลบด้วย) และฝั่ง RLS
  ฐานข้อมูล (`students_write` policy บังคับ `is_admin()`; ยิง API ตรงก็ถูกบล็อก) — จำเป็นต้องคง
  `students_select` ให้ครูอ่านได้ต่อไป เพราะหน้า `entry.html`/`attendance.html` ต้องดึงชื่อนักเรียน
  มาแสดงตอนกรอกคะแนน/เช็คชื่อ ถ้าปิดอ่านจะพังหน้าอื่นด้วย
- **ผู้ใช้ยืนยันเลือก “คงเดิม”** (ถามผ่าน AskUserQuestion): ครูดูรายชื่อได้อย่างเดียว เขียน/ลบ
  เฉพาะ admin ตามที่เป็นอยู่แล้ว — **ไม่มีการแก้โค้ด/RLS/schema เพิ่มจากประเด็นนี้**
- สถานะ deploy ไม่เปลี่ยน: โมดัลลบนักเรียนยังอยู่ในโฟลเดอร์พัฒนาเท่านั้น ยังไม่ได้ push ขึ้น
  GitHub Pages — เว็บที่ครูใช้จริงยังเป็นโค้ดเก่าที่ไม่มี modal นี้และ guard เดิม (บล็อกลบเฉย ๆ
  ไม่บอกรายละเอียด) จนกว่าจะ deploy รอบถัดไป
- เซิร์ฟเวอร์ทดสอบท้องถิ่น (`localhost:8787`) ยังรันค้างอยู่เผื่อทดสอบเพิ่มเติม

## 2026-07-18 — Deploy โมดัลลบนักเรียนขึ้นเว็บจริงแล้ว

- ผู้ใช้สั่ง “deploy เลย” หลังทดสอบผ่านแล้วที่ localhost — ตรวจโฟลเดอร์ deploy
  (`.../ไดรฟ์ของฉัน/งานวิชาการ/วัดผล/ProjectScore/CRS_Score`) ก่อนแตะ: เป็น git repo จริง
  (`origin` = `github.com/nonpatan/CRS_Score.git`), `git status` สะอาด (up to date กับ
  `origin/main`) ก่อนเริ่ม จึงไม่มีงานค้างของผู้ใช้ที่จะถูกทับ
- เทียบไฟล์ dev vs deploy ด้วย md5 ทุกไฟล์ พบต่างกันแค่ 4 ไฟล์ตามที่คาด (`students.html`,
  `AGENTS.md`, `CLAUDE.md`, `WORKLOG.md`) — ไม่มีไฟล์แปลกปลอม; ไฟล์ที่มีเฉพาะใน deploy
  (`logo.png`, `package.json`, `package-lock.json`, `test_supabase.js`, `.gitattributes`) ไม่แตะ
- ตรวจ diff ของ `students.html` ก่อน copy ยืนยันว่าเป็นการ**เพิ่ม**โค้ด modal อย่างเดียว
  ไม่มีอะไรหาย แล้ว copy ทั้ง 4 ไฟล์ทับของเดิมใน deploy folder
- Commit `c944dfc` (“Show linked records before deleting a student”) และ `git push origin main`
  สำเร็จ (`28f8788..c944dfc main -> main`)
- ตรวจยืนยันเว็บ live ด้วย md5 หลัง push: รอบแรก (~10 วิ) ยังเป็นไฟล์เก่า, รอบสอง (~20 วิ)
  **ตรงกับไฟล์ dev ทั้ง 4 ไฟล์แล้ว** — GitHub Pages build เสร็จและ deploy สำเร็จจริง
- ปิดเซิร์ฟเวอร์ทดสอบท้องถิ่น (`localhost:8787`) แล้วเพราะทดสอบและ deploy เสร็จสมบูรณ์
- **สถานะล่าสุด**: dev folder = deploy repo (`origin/main`) = เว็บ live ตรงกันครบทั้ง 4 ไฟล์
  ฟีเจอร์แสดงรายละเอียด+ลบนักเรียนทั้งหมดพร้อมใช้งานจริงแล้วที่
  `https://nonpatan.github.io/CRS_Score/students.html`

## 2026-07-18 — บอกชัดว่าสมรรถนะหลักแต่ละด้านเต็ม 100 เสมอ (`competency-manage.html`)

- ผู้ใช้ถามจากภาพหน้าจอ “กำหนดสมรรถนะหลัก” ว่าตัวสมรรถนะหลัก (ด้าน) ไม่มีการกำหนดคะแนนเต็ม
  เหรอ — ตรวจโค้ดจริง (`competency_assessment_targets`, `computeAssessmentCompetencySource`
  ใน `supabase-client.js`) ยืนยันว่า **ตั้งใจออกแบบแบบนี้**: คะแนนเต็มตั้งได้แค่ระดับองค์ประกอบ
  (“เต็ม 10” ต่อบรรทัด) ส่วนคะแนนของด้านเป็นสัดส่วนอัตโนมัติ
  `percent = rawSum(องค์ประกอบ) / maxSum(องค์ประกอบ) × 100` เสมอ ไม่ต้องกำหนดคะแนนเต็มของด้าน
  แยกอีกชั้น — ผู้ใช้ยืนยันเข้าใจถูกต้องแล้วว่า “ไม่ว่าจะเพิ่มองค์ประกอบเท่าไหร่ รวมแล้วใช้
  บัญญัติไตรยางศ์ไม่เกิน 100 เสมอ” แต่ขอให้ **เขียนบอกไว้ในหน้าเว็บด้วย** ให้ครูเห็นชัดเจน
  ไม่ต้องเดา/ถามซ้ำ
- แก้ `competency-manage.html`: หัวข้อชื่อด้าน (เช่น “การจัดการตนเอง”) ในการ์ดรายการองค์ประกอบ
  (`renderTargets()` บรรทัด ~355) เพิ่มข้อความรอง “คะแนนรวมเต็ม 100 (เทียบสัดส่วนจากองค์ประกอบ
  ด้านล่างอัตโนมัติ)” ต่อท้ายชื่อด้าน ด้วย `<span class='competency-target-cap'>` (ตัวเล็ก สีเทา
  มัวเหมือน `.field-note`/`.meta` ที่อื่นในหน้านี้) ไม่กระทบ logic คำนวณใด ๆ เป็นแค่ label
- ตรวจผ่าน: `node --check` module script ผ่าน; ตรวจว่าทั้ง CSS (`.competency-target-cap`) และ
  JS (`renderTargets()`) แก้ตรงจุดจริงด้วย grep ยืนยันซ้ำ
- ทดสอบภาพด้วย mock ไฟล์ในโฟลเดอร์ (ลบทิ้งหลังใช้แล้ว) — Browser pane แสดงผลสับสน (ไปโหลด
  หน้าเว็บจริงที่ต้องล็อกอินแทน mock) จึงไม่ได้ยืนยันภาพจริงในรอบนี้ ต้องให้ผู้ใช้ดูเองผ่าน
  local server (`localhost:8787`, เปิดใหม่อีกรอบ) หรือหลัง deploy
- **ยังไม่ deploy** — แก้เฉพาะโฟลเดอร์พัฒนา ยังไม่ได้ copy ไป deploy repo/push

## 2026-07-18 — แก้ 5 ข้อจาก Google Doc ผู้ใช้ (`competency-manage.html`)

- ผู้ใช้ส่งลิงก์ Google Doc "ปรับปรุงโปรแกรม" (แชร์แบบทุกคนที่มีลิงก์เข้าถึงได้) ระบุ 5 จุดที่ต้อง
  แก้ในหน้า "กำหนดสมรรถนะหลัก" — ดึงเนื้อหาด้วย `.../export?format=txt` (Browser pane อ่าน DOM
  ตรงไม่ได้เพราะ Google Docs render เป็น canvas; get_page_text/read_page ดึงไม่ได้ทั้งคู่) ได้
  ข้อความครบรวมถึงบรรทัดที่ถูกภาพแทรกบังไว้ในหน้าจอ
- ตรวจโค้ดจริงก่อนแก้ ยืนยันทั้ง 5 จุดเป็นของจริงไม่ใช่เข้าใจผิด: ไม่มีตัวกรองปี/เรียงชื่อในรายการ
  (เรียงแค่ created_at), หน้านี้ไม่เคย import `competencyStageForGrade` เลยจึงไม่เช็คช่วงชั้น
  ตอนเพิ่มผู้เข้าร่วม, ข้อความ empty-state ใช้ `.empty` (เทาจาง) เดียวกันหมด, ฟอร์มสร้างรายการ
  ไม่มีช่องเจ้าของ/ครูผู้รับผิดชอบเลย (owner_id ตั้งเป็นผู้สร้างเสมอ)
- ถามผู้ใช้ยืนยัน 3 จุดที่กำกวมก่อนลงมือ (AskUserQuestion): (1) ตัวกรองในรายการ **ไม่ต้องกรอง
  ตามชั้น** เพราะกิจกรรม/กิจวัตรคละชั้นได้ → กรองปี + เรียงชื่อพอ (2) นักเรียนช่วงชั้นที่ไม่มี
  องค์ประกอบรองรับ **บล็อกไม่ให้เพิ่ม + ขึ้นข้อความเตือน** (3) "ครูผู้รับผิดชอบ" **เฉพาะ Admin
  เลือก/เปลี่ยนได้** mirror ตาม `manage.html` ช่อง "เจ้าของวิชา" (owner_id ผูกสิทธิ์แก้ไขโดยตรง
  ผ่าน `canManageAssessment()`)
- Implement ครบทั้ง 5 ข้อใน `competency-manage.html`:
  1. เพิ่ม dropdown "กรองปีการศึกษา" ในการ์ด "รายการที่กำหนดไว้" (ค่าเริ่มต้น = ปีที่ใช้งานอยู่ ถ้า
     มีในรายการ ไม่งั้น "ทุกปี") + `renderAssessments()` เรียงตามชื่อ (`localeCompare` ภาษาไทย)
     แทนการพึ่ง sort จาก query อย่างเดียว — เพิ่ม `refreshAssessmentYearFilter()` เรียกซ้ำตอนมีปี
     ใหม่โผล่ (ทั้งตอนสร้างรายการและแก้ไขรายการ) กันตัวกรองไม่ทันอัปเดต
  2. `renderStudents()` เพิ่ม `targetStageSet()` (รวมช่วงชั้นจาก `targets` ทุกสมรรถนะในรายการ)
     เทียบกับ `competencyStageForGrade(s.grade_level)` (import จาก supabase-client.js เพิ่ม) —
     นักเรียนช่วงชั้นที่ไม่ตรงถูก `disabled` checkbox + ขึ้นข้อความ "ไม่มีองค์ประกอบรองรับ...
     — เพิ่มองค์ประกอบก่อนจึงเพิ่มนักเรียนคนนี้ได้" ใต้ชื่อ (dim ด้วย opacity ผ่าน class `.blocked`)
     — **แก้บั๊กแฝงที่เจอระหว่างทำ**: ปุ่ม "+ เพิ่มทั้งห้องตามตัวกรอง" เดิมดึง `querySelectorAll
     ("input")` ทุกช่องไม่สนใจ disabled จะได้นักเรียนที่ถูกบล็อกติดไปด้วยอยู่ดี ต้องเปลี่ยนเป็น
     `input:not(:disabled)` (และ `btn-add-selected` ก็เผื่อไว้ด้วย `:checked:not(:disabled)`)
     ไม่งั้นบล็อกที่ปุ่มติ๊กแยกจะไม่มีผลกับปุ่มเพิ่มทั้งห้อง
  3. (ต่อเนื่องข้อ 2) ครอบคลุมแล้วในจุดเดียวกัน — ตัวกรองชั้น/ห้องยังใช้ได้ตามเดิม แต่แถวที่ถูก
     บล็อกจะโชว์เหตุผลชัดเจนแทนการปล่อยให้กดติ๊กได้เฉยๆ โดยไม่รู้ว่าจะไม่ได้คะแนน
  4. เพิ่มคลาส `.empty-note` (สีอำพัน `--amber`/`--amber-soft` ตัวหนา ยืมชุดสีเดียวกับ
     `students.html`'s readonly-note) ใช้แทน `.empty` เฉพาะ 2 ข้อความ "ยังไม่ได้เลือกองค์ประกอบ"
     และ "ยังไม่มีผู้เข้าร่วม" — จุดอื่นที่ใช้ `.empty` (กำลังโหลด, ไม่มีรายชื่อให้เพิ่ม ฯลฯ) คงเดิม
  5. เพิ่มช่อง "ครูผู้รับผิดชอบ" (`#f-owner`) ในฟอร์ม "สร้างรายการใหม่" และ (`#assessment-edit-
     owner`) ใน modal แก้ไขรายการ — **admin-only** (`style="display:none"` ปลดล็อกใน `init()`
     เมื่อ `isAdmin`) เติมจาก `profiles` (เพิ่ม `role` เข้า select query เพื่อต่อท้าย "(admin)"
     เหมือน `manage.html`) ค่าเริ่มต้นตอนสร้าง = ตัวเอง; `btn-create`/`assessment-edit-save` ส่ง
     `owner_id` เฉพาะเมื่อ isAdmin (ครูทั่วไปยังคงเป็นเจ้าของอัตโนมัติเหมือนเดิม ไม่กระทบ flow เดิม)
     — ไม่ล็อกช่องนี้แม้รายการจะมีครั้งประเมินแล้ว (ต่างจาก ปี/ประเภท/ภาคเรียน) เพราะเปลี่ยนเจ้าของ
     ไม่กระทบ snapshot/ประวัติคะแนนใด ๆ เป็นแค่เรื่องสิทธิ์แก้ไข — ไม่ต้องแก้ RLS/schema เพิ่ม
     (`can_edit_competency_assessment` เช็ค owner_id แบบ row-level อยู่แล้ว ไม่สนว่าคอลัมน์ไหนถูก
     update)
- ตรวจผ่าน: `node --check` module script ผ่าน; parse ทั้งไฟล์ด้วย Python `html.parser` ยืนยัน
  ไม่มี tag ค้าง/ปิดไม่ตรง; grep ยืนยันทุก element id ที่ JS เรียกมีอยู่จริงใน HTML ครบ (ไม่มี id
  ตกหล่น); ตรวจ RLS write policy ของ `competency_assessments` แล้วว่าเป็น row-level (ไม่ใช่
  column-level) จึง admin อัปเดต owner_id ของรายการคนอื่นได้แน่นอนโดยไม่ต้องแก้ policy
- ทดสอบภาพจริงในนี้ไม่ได้อีกเช่นเดิม (Browser pane ในเซสชันนี้เปิดหน้าเว็บจริงที่ต้องล็อกอินแทน
  mock ทุกครั้ง) — เซิร์ฟเวอร์ทดสอบท้องถิ่นเดิม (`localhost:8787`) ยังรันอยู่ ให้ผู้ใช้ทดสอบเอง
  ก่อน deploy จริง
- **ยังไม่ deploy** — ทุกอย่างอยู่ในโฟลเดอร์พัฒนาเท่านั้น

## 2026-07-18 — Deploy 5 จุด competency สำเร็จ + revert งานเช็คชื่อรายวัน (กลับสภาพก่อนหน้า)

> หมายเหตุ: entry นี้เขียนหลัง revert เพื่อสรุปสถานะให้ถูกต้อง (WORKLOG.md ในชุด deploy commit
> `0a4ff32` ยังไม่มีบันทึกช่วงนี้ เพราะเขียนหลัง push — ไม่กระทบการทำงานเว็บ)

- **การแก้ 5 จุดจาก Google Doc ที่ `competency-manage.html` ถูก deploy แล้วจริง** (commit
  `0a4ff32` "Improve competency activity management page", push ขึ้น `origin/main` สำเร็จ,
  ตรวจ md5 เว็บ live ตรงกับ dev ครบ 4 ไฟล์: competency-manage.html/AGENTS.md/CLAUDE.md/WORKLOG.md)
  — ข้อความ "ยังไม่ deploy" ในหัวข้อก่อนหน้าเป็นสถานะ**ก่อน** push เท่านั้น ตอนนี้ deploy แล้ว
- **งานเช็คชื่อรายวัน (เฟส 3) ถูก revert ทั้งหมดตามคำสั่งผู้ใช้**: ผู้ใช้ระบุว่าที่พิมพ์ก่อนหน้า
  คือให้ **เสนอแนวคิดก่อน ไม่ใช่ให้ลงมือทำ** (Claude เผลอสร้าง schema+daily.html+แก้ 14 ไฟล์ไปเอง)
  — คืนสภาพโดย copy 13 ไฟล์กลับจาก deploy repo (สถานะ `0a4ff32` = ก่อนรอบนี้เป๊ะ) + ลบ `daily.html`
  ยืนยันด้วย md5 ว่า dev ตรงกับ deploy ทุกไฟล์แล้ว (schema.sql/app-shell.js/attendance/competency-
  entry/competency-manage/entry/manage/retention/rollover/students/summary/warning + version bump
  `?v=20260718`→`?v=20260716-2` คืนหมด) · ตารางเช็คชื่อรายวัน **ไม่เคยถูกสร้างบน Supabase**
  (migration ยังไม่รัน) เว็บ live ไม่เคยมีอะไรเปลี่ยน — ปลอดภัย
- **บทเรียน (จดเป็น memory `confirm-before-build.md` แล้ว)**: "เสนอ/ลองเสนอ/ความเห็น/แนวคิด" =
  เสนอแล้วรอยืนยันก่อนเสมอ ไม่ลงมือ · "ทำเลย/push เลย" = ลงมือ

### วิสัยทัศน์จริงของผู้ใช้ (Dashboard กลาง) — ยังไม่เริ่ม รอเสนอแผน+ยืนยันก่อนทำ

ผู้ใช้อยากได้ **หน้า Dashboard กลาง** เป็นศูนย์รวม:
- เมนูแยกเข้าแต่ละฝ่าย: **ฝ่ายวิชาการ** / **ฝ่ายบริหารทั่วไป** / **ฝ่ายบุคคล**
- แสดง**ข้อมูลประจำวัน**บนแดชบอร์ด เช่น นักเรียนที่มาวันนี้, นักเรียนที่ไม่มาแยกรายห้อง,
  นักเรียนที่เสี่ยงติด มส. (บอกว่าคนไหน วิชาอะไร)

> รอบถัดไป: **เสนอแผน/โครงสร้าง Dashboard ก่อน แล้วหยุดรอผู้ใช้ยืนยัน** ค่อยลงมือ (ห้ามสร้างเอง)
> — ของที่มีอยู่แล้วดึงมาโชว์ได้เลย (เสี่ยง มส. มี `warning.html`/`computeMissedPeriods`) ส่วน
> "มาวันนี้/ไม่มารายห้อง" ต้องมีเช็คชื่อรายวัน (ยังไม่มีในระบบ — เคยลองทำแล้ว revert ไป)

## 2026-07-18 — แยกโครงสร้างไฟล์ตาม 3 ฝ่าย (ยังไม่สร้าง Dashboard)

- ผู้ใช้ยืนยันให้ทำเฉพาะการจัดโครงสร้างเป็น 3 ฝ่ายก่อน: ข้อมูล “ขาดบ่อย” จะใช้เกณฑ์เปอร์เซ็นต์
  เดียวกับความเสี่ยง มส. ในงาน Dashboard ภายหลัง, “มาวันนี้” เป็น placeholder ของ v1, และห้าม
  สร้าง `dashboard.html` หรือระบบเช็คชื่อรายวันในรอบนี้
- ย้ายหน้าที่มีอยู่ทั้งหมด 10 หน้าเข้า `academic/` ได้แก่ กรอกคะแนน, เช็คชื่อ, สรุปผล, จัดการ
  โครงสร้าง, นักเรียน, ขึ้นปีใหม่, เฝ้าระวัง มส., เรียนซ้ำชั้น, กำหนดสมรรถนะ และลงคะแนนสมรรถนะ
  — `general-affairs/` และ `personnel/` ถูกสร้างเป็นพื้นที่ว่างรอโมดูลในอนาคต
- คง `index.html`, `login.html`, `supabase-client.js` และ `app-shell.*` ที่รากเว็บ. URL เดิมของ
  หน้าวิชาการ (`entry.html`, `attendance.html` ฯลฯ) ถูกคงเป็นหน้า redirect เพื่อไม่ให้ bookmark/
  ลิงก์เดิมของครูเสีย. ปรับ asset/module import ของหน้าที่ถูกย้ายเป็น `../...`
- ปรับ `requireAuth()`/`signOut()` ให้หา login ที่รากเว็บจากตำแหน่งไฟล์กลางเสมอ, ปรับ login
  allowlist ให้รับทั้ง URL เดิมและ `academic/...`, และแก้ sign-out เฉพาะของ `warning.html` ให้ใช้
  helper กลางเดียวกัน
- ตรวจผ่าน: `node --check app-shell.js` และ `node --check supabase-client.js`; ตรวจทุก import,
  stylesheet, script และ redirect ที่อ้าง path ใหม่ด้วย `rg`. ยังไม่ได้ deploy
- ทดสอบเว็บท้องถิ่นจริง: ทั้ง URL เก่า `/entry.html?source=legacy` และ URL ใหม่
  `/academic/entry.html?source=new` โหลด `app-shell.css`, `app-shell.js` และ
  `supabase-client.js` ได้ HTTP 200 แล้วพาไป `login.html?next=academic/...` ถูกต้อง; browser
  console ไม่มี error. พบเฉพาะ `logo.png` 404 ในโฟลเดอร์พัฒนา ซึ่งเป็นข้อสังเกตเดิม (ชุด deploy
  และเว็บ live มีไฟล์ logo อยู่แล้ว) ไม่ใช่ผลจากการย้ายโฟลเดอร์

## 2026-07-18 — Dashboard mobile-first + ข้อมูลส่วนตัวผู้ใช้

- ผู้ใช้ยืนยันให้สร้าง Dashboard จริง โดยย้ำว่ามือถือและ iPad เป็นอันดับแรก และเพิ่ม requirement
  ให้ผู้ที่ login เห็นรูปย่อ+ชื่อของตัวเอง พร้อมแก้ข้อมูลส่วนตัวได้
- เพิ่ม `dashboard.html` ที่รากเว็บ: มือถือใช้การ์ด 1 คอลัมน์, tab 4 รายการ (ภาพรวม/วิชาการ/
  บริหารทั่วไป/บุคคล) และ bottom nav; iPad (≥700px) เปลี่ยนเป็น sidebar, การ์ด 3 ใบหนึ่งแถว และ
  รายชื่อ 2 คอลัมน์. “มาเรียนวันนี้” เป็น placeholder v1 ตามที่ล็อกไว้; “เสี่ยงติด มส.” และ
  “ขาดบ่อย” ใช้ threshold ขาดสะสม ≥10% เดียวกัน และดึงรายชื่อจริงพร้อมวิชาที่ต้องติดตาม
- Dashboard โหลดวิชาใน active year, enrollments, session การเช็คชื่อ และ makeup hours แบบ batch;
  ใช้ `computeMissedPeriods()` (ขาดเต็ม/ลาป่วย-ลากิจครึ่ง, หักชั่วโมงชดเชย) และตรรกะวิชาบูรณาการ
  แบบเดียวกับ `warning.html` เพื่อไม่สร้างสูตร มส. ชุดใหม่
- เพิ่ม `profile.html` ให้ผู้ใช้แก้ “ชื่อที่แสดง” และเลือกรูป JPG/PNG/WebP ไม่เกิน 2 MB; ชื่อ/รูป
  ไม่ปะปนกับ `profiles` ที่เก็บ role/email และไม่ให้ครูแก้สิทธิ์ตัวเอง. `app-shell.js` ทุกหน้า
  วิชาการเพิ่มปุ่มรูปย่อ+ชื่อที่ลิงก์มาหน้านี้; ถ้าไม่มีรูปใช้ชื่อ/อักษรย่อเป็น fallback
- เพิ่ม migration ท้าย `schema.sql`: ตาราง `user_profile_details` (RLS self-only) + private bucket
  `profile-avatars` พร้อม policy read/write เฉพาะไฟล์ใต้โฟลเดอร์ UUID ของเจ้าของ. **ยังไม่รัน SQL
  นี้บน Supabase** — ต้องรันก่อนจึงจะบันทึกชื่อหรืออัปโหลดรูปจริงได้; Dashboard ยังแสดง fallback
  จาก profiles/email ได้แม้ migration ยังไม่รัน
- ปรับ login default ให้เข้า `dashboard.html`; allowlist รองรับ `dashboard.html`/`profile.html`
  และ path `academic/...` เดิม. ตรวจผ่าน `node --check` สำหรับ JS กลางและ module script ทั้งสองหน้า;
  ทดสอบ localhost ว่า Dashboard, Profile และหน้า academic ส่งไป login พร้อม `next` ถูกต้องและไม่มี
  console error. การตรวจ visual หลัง login รอ session ของผู้ใช้ เพราะ agent ไม่มีบัญชีสำหรับทดสอบ

## 2026-07-18 — ปรับหน้า Login เป็น Modern School Portal

- ปรับเฉพาะ `login.html` โดยคง Supabase login flow, ชื่อ id ของฟอร์ม และ allowlist เดิมครบทั้งหมด
  (ไม่มีการแก้ schema, RLS หรือ logic การยืนยันตัวตน)
- เปลี่ยนภาพรวมเป็นธีมเขียวเข้มร่วมสมัย: พื้นหลังมีมิติแบบ grid/aurora, โลโก้มีวงแหวน, การ์ดล็อกอิน
  มีแถบสีแบรนด์, ช่องกรอกมีสัญลักษณ์ช่วยมอง และปุ่มเข้าสู่ระบบเด่นขึ้น — มือถือยังเป็นหนึ่งคอลัมน์
  และ iPad ขึ้นไปแสดงสองคอลัมน์ตามเดิม
- ตรวจผ่าน: module script ใน `login.html` ผ่าน `node --input-type=module --check`; reload หน้า
  `http://127.0.0.1:8787/login.html` สำเร็จบน local preview
- **ยังไม่ deploy** — งานอยู่ในโฟลเดอร์พัฒนาและ local preview เท่านั้น

## 2026-07-18 — คืนรูปแบบหน้า Login เดิม

- ผู้ใช้ดู local preview แล้วเลือกกลับไปใช้รูปแบบก่อนปรับ Modern School Portal จึงคืนสี พื้นหลัง
  ขนาดโลโก้ การ์ด ฟอร์ม และข้อความประกอบกลับเป็นแบบเดิมทั้งหมด
- คงเฉพาะ logic login ที่มีอยู่เดิมไว้ครบ (Supabase, id ของฟอร์ม, safe next-page allowlist)
  โดยไม่มีการแก้ schema, RLS หรือข้อมูลใด ๆ
- ตรวจผ่าน: module script ยังผ่าน `node --input-type=module --check` และ reload local preview
  `http://127.0.0.1:8787/login.html` สำเร็จ
- **ยังไม่ deploy** — การคืนรูปแบบมีผลเฉพาะโฟลเดอร์พัฒนา/local preview

## 2026-07-18 — เลือกตำแหน่งรูปโปรไฟล์ก่อนบันทึก

- ปรับ `profile.html` ให้การเลือกรูปเปิดหน้าจอจัดตำแหน่งก่อนเสมอ: มีกรอบวงกลมเป็นพื้นที่ที่
  จะเห็นในระบบ, ลากภาพด้วยนิ้วหรือเมาส์ได้, และมีแถบซูม 100–300% ที่ใช้ได้ด้วยคีย์บอร์ด
- เมื่อกด “ใช้รูปนี้” ระบบสร้างไฟล์รูปสี่เหลี่ยม 512×512 จากตำแหน่งที่เลือก (WebP คุณภาพสูง;
  ถ้าเบราว์เซอร์สร้าง WebP ไม่ได้จึง fallback เป็น JPEG) แล้วอัปโหลดเฉพาะไฟล์ที่ครอบแล้วลง
  private bucket เดิมเมื่อผู้ใช้กด “บันทึกข้อมูลส่วนตัว” — ไม่ต้องเพิ่มตาราง, Storage policy หรือ
  schema ใหม่
- ยกเลิกหรือกด Escape ได้โดยไม่เปลี่ยนรูปเดิม; หากเลือกแล้วแต่ยังไม่กดบันทึก ข้อมูลยังอยู่เฉพาะ
  ในหน้าเว็บ ไม่ถูกอัปโหลด
- ตรวจผ่าน: module script ผ่าน `node --input-type=module --check`; local preview มี dialog,
  พื้นที่ลาก และแถบซูมครบหลัง reload `http://127.0.0.1:8787/profile.html`
- **ยังไม่ deploy** — ผู้ใช้ต้องทดสอบเลือกรูปจริงใน local preview ก่อน

## 2026-07-18 — แก้เมนูวิชาการยืดเต็มหน้าจอ

- ผู้ใช้พบที่ `academic/entry.html` ว่าเมนูส่วนกลางขยายสูงเกือบเต็มหน้าจอแทนที่จะเป็นแถบเมนู
  ขนาดกระชับ — สาเหตุจาก `app-shell.css` สั่ง `align-items: stretch` ให้ nav ที่ app-shell.js
  เปลี่ยนเป็นกลุ่มเมนูแนวตั้ง ทำให้กลุ่มยืดตามความสูงของ container ในบาง viewport
- แก้ที่ `app-shell.css` จุดเดียว: กำหนด `height:auto`, `min-height:0`, `align-items:flex-start`
  และจัดกลุ่ม/ลิงก์ภาพรวม/ชื่อผู้ใช้/ออกจากระบบชิดด้านบนทั้งหมด จึงมีผลกับทุกหน้าวิชาการร่วมกัน
- ตรวจผ่าน: reload `http://127.0.0.1:8787/academic/entry.html` บน local preview สำเร็จ
- **ยังไม่ deploy** — รอผู้ใช้ตรวจ local preview ก่อน

## 2026-07-18 — เอาปุ่มผู้ใช้จากเมนูวิชาการ

- ผู้ใช้ยืนยันให้เอาปุ่มรูป+ชื่อผู้ล็อกอินที่เคยอยู่ติดกับ “ออกจากระบบ” ออกจากเมนูทุกหน้า
  วิชาการ เพราะทำให้เมนูรกและเป็นสาเหตุหนึ่งของเลย์เอาต์ที่ดูใหญ่
- แก้ `app-shell.js` ให้เมนูเหลือภาพรวม, กลุ่มงาน และออกจากระบบ; ตัดการโหลด profile/รูปออกจาก
  หน้าวิชาการด้วย. หน้าตั้งค่าข้อมูลส่วนตัวยังเข้าผ่านรูปบัญชีที่หน้า Dashboard ตามเดิม
- ลบ CSS ที่ใช้เฉพาะ user menu แล้ว bump cache version ของ `app-shell.css`/`app-shell.js` ในหน้า
  วิชาการทั้ง 10 หน้าเป็น `20260718-2` เพื่อให้เบราว์เซอร์ไม่คงเมนูรุ่นเก่า
- ตรวจผ่าน: `node --check app-shell.js`; ตรวจครบ 10 หน้าใช้ version ใหม่; reload local entry
  แล้ว DOM เหลือ “ออกจากระบบ” โดยไม่มีลิงก์ profile ในเมนู
- **ยังไม่ deploy** — อยู่ใน local preview เท่านั้น

## 2026-07-18 — แก้รูปโปรไฟล์แสดงแหว่ง

- ผู้ใช้พบที่ `profile.html` ว่ารูปที่เลือกถูกแสดงครึ่งล่าง ขณะที่อักษรย่อค้างอยู่ครึ่งบน
  สาเหตุคือ `<span>` อักษรย่อและ `<img>` ถูกวางเป็นคนละแถวใน CSS grid เดียวกัน
- ปรับทั้งสองส่วนให้ซ้อนตำแหน่งเดียวกันภายในวงกลม และให้อินพุตไฟล์อยู่ชั้นบนสุดเพื่อแตะเปลี่ยนรูปได้
  ตามเดิม; รูปใช้ `object-fit: cover` จึงเต็มวงกลมโดยไม่บิดสัดส่วน
- ปรับ `showAvatar()` ให้ซ่อนอักษรย่อทุกครั้งที่มี URL รูป จึงไม่มีอักษรย่อทับรูปอีก
- ตรวจผ่าน: module script ผ่าน `node --input-type=module --check`; reload local `profile.html`
  แล้วพบว่าอักษรย่อว่าง, รูปแสดงอยู่, อยู่แบบ absolute และ `object-fit:cover`
- **ยังไม่ deploy** — แก้ใน local preview เท่านั้น

## 2026-07-18 — ใส่โลโก้บน Dashboard

- ผู้ใช้ขอให้แทนตราอักษรย่อ “ศก” บน Dashboard ด้วยโลโก้โรงเรียนจริง จึงใช้ไฟล์ `logo.png`
  ที่มีอยู่ในโครงการ (500×500) โดยไม่สร้างหรือดัดแปลงภาพใหม่
- ปรับทั้งแถบด้านบนสำหรับมือถือ และตราที่ sidebar สำหรับ iPad/เดสก์ท็อปให้ใช้โลโก้เดียวกัน;
  กำหนดขนาด 36px/32px, ทรงกลม และ `object-fit:contain` เพื่อให้ตราไม่ถูกตัดหรือบิด
- ตรวจว่าไฟล์โลโก้มีอยู่และ Dashboard อ้างอิงครบทั้ง 2 ตำแหน่ง; ไม่มีอักษร “ศก” หลงเหลือ
- **ยังไม่ deploy** — แก้ในโฟลเดอร์พัฒนา/local preview เท่านั้น

## 2026-07-18 — ปิดงานเติมข้อมูลวิชาเก่า

- ผู้ใช้ยืนยันว่าได้กรอกข้อมูลวิชาเก่า “วิทยาการคำนวณ” ครบแล้ว ได้แก่ รหัสวิชา, ชั้นเรียน
  และครูเจ้าของวิชา จึงนำรายการนี้ออกจากงานค้างใน `AGENTS.md` เพื่อไม่ให้ agent รอบถัดไป
  รายงานผิดว่าเป็นงานที่ต้องทำอีก
- เป็นการอัปเดตสถานะตามที่ผู้ใช้ยืนยันเท่านั้น ไม่ได้แก้ข้อมูลวิชาในฐานข้อมูลหรือ deploy เว็บ

## 2026-07-18 — ยืนยันสถานะการทดสอบรายงานย้อนหลังหลัง rollover

- ผู้ใช้ทักว่ามีการทดสอบไปแล้ว จึงตรวจ worklog ย้อนหลังและยืนยันว่ามีการทดสอบ end-to-end จริงเมื่อ
  2026-07-17: rollover ปี 2569 ไปปีทดสอบ 9999, ตรวจ `summary.html` กลับไปปี 2569 แล้วพบรายชื่อ
  นักเรียนเดิม 20 คนแม้สถานะปัจจุบันถูกตั้งเป็นจบระหว่างทดสอบ
- ข้อมูลทดสอบปี 9999 ถูกล้างและคืนค่า placement/ชั้น/ห้อง/สถานะของปี 2569 ครบแล้ว จึงไม่เหลือ
  งานทดสอบรายงานย้อนหลังเป็นรายการค้าง; เพิ่มข้อความสรุปไว้ใน `AGENTS.md` เพื่อไม่ให้รายงานผิดซ้ำ

## 2026-07-18 — Deploy Dashboard และโครงสร้าง 3 ฝ่าย

- ผู้ใช้สั่ง push จึงคัดลอกเฉพาะไฟล์ CRS Score จากโฟลเดอร์พัฒนาไปยัง repo deploy
  `nonpatan/CRS_Score` โดยตรวจว่า worktree ปลายทางสะอาดก่อนเริ่ม
- Deploy ครอบคลุมโครงสร้าง `academic/` พร้อม URL เก่าสำหรับส่งต่อ, Dashboard, Profile พร้อม
  การจัดตำแหน่งรูป, โลโก้โรงเรียน, เมนูวิชาการที่กระชับ และเอกสารสถานะล่าสุด
- ตรวจผ่านก่อน commit: `git diff --cached --check`, `node --check app-shell.js` และ
  `node --check supabase-client.js`; เก็บ trailing whitespace ในไฟล์ที่ย้ายแล้ว
- commit `e374706` (`Add school dashboard and profile settings`) ถูก push ขึ้น `main` สำเร็จ
  จาก `0a4ff32`; GitHub Pages จะรับชุดนี้ไป deploy ตามปกติ

## 2026-07-18 — ปรับหน้าตาฝ่ายวิชาการเป็น Academic Workspace

- ปรับ `app-shell.css` ที่ใช้ร่วมกันทั้ง 10 หน้าฝ่ายวิชาการ โดยไม่แตะ schema, Supabase,
  สูตรคำนวณ หรือ event/ID ของฟอร์ม: หัวหน้าแต่ละหน้ากลายเป็นการ์ดสรุปแบบ modern, การ์ดข้อมูลมี
  surface/เงา/มุมที่สม่ำเสมอ, ฟอร์มสูง 48px เพื่อแตะง่าย, และปุ่มหลัก/รอง/เตือนมีลำดับชัดขึ้น
- ปรับเมนูกลางให้เป็นแถบเครื่องมือแบบมีหมวดและเลื่อนแนวนอนแถวเดียวทั้งมือถือและ iPad; ตรวจใน
  browser แล้วพบครั้งแรกว่ายัง wrap หลายแถวจึงแก้เป็น `flex-wrap: nowrap` และ bump cache
  `app-shell.css`/`app-shell.js` ในหน้าวิชาการครบ 10 หน้าเป็น `20260718-4`
- `app-shell.js` เพิ่ม class และ aria-label ให้ header/menu เพื่อให้โครงสร้างอ่านง่ายขึ้นโดยไม่
  เปลี่ยนรายการเมนูหรือ URL เดิม
- ตรวจผ่าน: `node --check app-shell.js`, `node --check supabase-client.js`, และยืนยันว่าหน้า
  วิชาการทั้ง 10 หน้าเรียก asset version ใหม่ครบ. Local browser แสดงเมนูและฟอร์มปกติก่อน reload;
  หลัง reload session ทดสอบหมดอายุจึงกลับหน้า login (ไม่มีข้อมูลถูกแก้ไข)
- **ยังไม่ deploy** — รอผู้ใช้ดูหน้า Academic บน local หลังล็อกอินก่อน

## 2026-07-18 — ปรับ Academic Workspace ให้ต่างจากโครงเดิมชัดเจน

- ผู้ใช้ดู local แล้วทักว่าหน้าตายังคล้ายเดิม จึงปรับรอบสองจากการขัดเงา CSS เป็นโครง workflow
  จริง: app-shell.js เติมหัวข้อ “WORKFLOW”, คำอธิบายตามงาน และขั้นตอน 1–3 ให้การ์ดเริ่มต้นของ
  ทุกหน้าฝ่ายวิชาการ โดยไม่แก้ id, form หรือ event เดิม
- หน้า entry.html แสดง “ตั้งค่าการกรอกคะแนน → ตั้งค่า / เลือกครั้ง / กรอกคะแนน” ก่อนตัวเลือก;
  หน้าอื่นใช้ข้อความ/ลำดับที่ตรงงาน เช่น เช็คชื่อ, รายงาน, จัดการวิชา และขึ้นปีใหม่
- ปรับการ์ด workflow ให้เด่นด้วยพื้นหลังสี mint และบน iPad จัดฟอร์มใน workflow card เป็นสองคอลัมน์
  เมื่อเหมาะสม; เมนูถูกล็อกเป็นแนวนอนแถวเดียวเพื่อไม่กลับไปสูงหลายแถว
- bump cache asset ในทั้ง 10 หน้าวิชาการเป็น 20260718-5; ตรวจผ่าน node --check
  ของ app-shell.js/supabase-client.js, ยืนยัน version ใหม่ครบ 10 หน้า, และตรวจ screenshot
  local ของ academic/entry.html เห็น workflow card จริง
- **ยังไม่ deploy** — ผู้ใช้กำลังตรวจ local preview

## 2026-07-18 — แก้ฟอร์มชั้นสูงสุดที่หน้า rollover บนมือถือ

- ผู้ใช้พบว่าการ์ด “ชั้นสูงสุดที่โรงเรียนเปิดสอน” ที่ rollover.html แสดง dropdown กับปุ่มบันทึก
  ชิดขวาและดูผิดเลย์เอาต์ สาเหตุคือ responsive rule เปลี่ยน field แบบ flex เป็นแนวตั้ง แต่ inline
  align-items:flex-end ของฟอร์มเดิมยังมีผลอยู่
- แก้ CSS กลางบนจอ ≤600px ให้ field แบบ flex ใช้ align-items:stretch และบังคับ child ที่เป็น
  flex:1 กับปุ่มให้กว้างเต็มการ์ด จึงได้ dropdown เต็มแถวและปุ่มบันทึกเต็มแถวตามลำดับ
- bump cache asset ในทุกหน้าวิชาการเป็น 20260718-6; ตรวจผ่าน node --check และ screenshot local
  ของ rollover.html ยืนยันว่าการ์ดแสดงตามปกติ
- **ยังไม่ deploy** — ผู้ใช้กำลังตรวจ local preview

## 2026-07-18 — เพิ่มการแสดงผลแบบนุ่มนวลในฝ่ายวิชาการ

- เพิ่ม motion แบบ fade-up ระยะสั้นให้ header และการ์ดที่แสดงทันทีในทั้ง 10 หน้าฝ่ายวิชาการ
  เพื่อให้การเข้าหน้าดูค่อยเป็นค่อยไป โดยไม่แตะข้อมูล ฟอร์ม หรือ logic การทำงานเดิม
- กรณีหน้า `rollover.html` ที่เปิดเนื้อหาหลังตรวจสิทธิ์ ให้การ์ดใน `#main` เริ่มแสดงหลังกล่องถูก
  เปิดจริง จึงไม่เกิดอาการกระพริบหรือค้างซ่อน
- รองรับการตั้งค่า `prefers-reduced-motion`: หากผู้ใช้เลือกให้ลดการเคลื่อนไหว ระบบจะแสดงเนื้อหา
  ทันทีตามปกติ
- bump cache ของ `app-shell.css`/`app-shell.js` ในทั้ง 10 หน้าเป็น `20260718-7`; ตรวจผ่าน
  `node --check app-shell.js`, `node --check supabase-client.js` และยืนยันว่าไม่มีหน้าใดอ้าง asset
  รุ่นก่อนค้างอยู่
- **ยังไม่ deploy** — รอผู้ใช้ดูผลบน local preview ก่อน

## 2026-07-18 — เพิ่ม motion ให้ Dashboard

- ผู้ใช้ทักว่าต้องการจังหวะแสดงผลใน Dashboard ด้วย จึงเพิ่ม fade-up แบบเดียวกับฝ่ายวิชาการให้
  แถบบน หัวเรื่อง การ์ดตัวเลข แผงรายชื่อ และแถบเมนู โดยเรียงหน่วงเวลาเล็กน้อยเพื่อให้ดูเป็นธรรมชาติ
- แถวรายชื่อนักเรียนที่โหลดจากฐานข้อมูลภายหลังจะค่อย ๆ ปรากฏเมื่อ render เสร็จ ไม่กระทบสูตร
  การคำนวณหรือข้อมูลที่แสดง
- ใช้ `prefers-reduced-motion` เช่นเดียวกับหน้าวิชาการ: ผู้ใช้ที่ลดการเคลื่อนไหวจะเห็นเนื้อหาทันที
- **ยังไม่ deploy** — รอตรวจ local Dashboard ก่อน

## 2026-07-18 — ออกแบบหน้า login ใหม่ (โมเดิร์น+สมูท) + ปรับ dashboard/หน้าวิชาการให้เข้าชุดกัน

> ผู้ใช้แยกงานให้ agent นี้ทำเรื่อง "หน้าตา" ให้ทั้งแอปสอดคล้องกัน และให้ agent นี้เป็นผู้ deploy
> (อีก agent ที่ทำ dashboard/academic/profile หยุดแล้ว และจะไม่ deploy เอง)

- **`login.html` — ออกแบบใหม่ทั้งหน้า (deploy แล้ว commit `561ca18`)**: ธีมสว่างเดิมที่ผู้ใช้ชอบ
  (ไม่กลับไปเข้มแบบที่เคย revert) + aurora พื้นหลังไหลช้า (orb 3 ก้อน blur) + เส้นตาราง blueprint
  จาง + entrance stagger + การ์ดกระจก + แถบ gradient teal→mint บนสุด + ช่อง input มีไอคอน (SVG)
  focus ring ลื่น + ปุ่มมีแสงกวาด (sheen) + ลูกศรเลื่อน + เคารพ `prefers-reduced-motion` ·
  **คง logic auth เดิมเป๊ะ** (getSafeNextPage รองรับ academic/ + dashboard/profile, allowlist,
  session redirect) — ตรวจแล้ว script ส่วน `<script type="module">` เหมือนเดิมทุกบรรทัด · ลบ CSS
  archive (ดีไซน์เข้มที่เคย revert) ที่ตายแล้วออก · ทดสอบ desktop+mobile + console ไม่มี error ·
  deploy เฉพาะ login.html ไฟล์เดียว (ตอนนั้น dev↔deploy ยัง out of sync หลายไฟล์จากงานอีก agent
  จึงจงใจไม่แตะไฟล์อื่น)
- **`dashboard.html` — polish หน้าตาให้เข้าชุดกับ login (แตะแค่ CSS)**: ยึด dev version (ใหม่กว่า
  deploy = งานล่าสุดอีก agent) เป็นฐาน · palette teal ตรงกับ login (`#0f6e56`/`#0a5644`/cyan/mint) ·
  พื้นหลัง gradient + aurora จาง (subtle เพราะเป็นหน้าข้อมูล) · metric cards เป็นกระจก + แถบ gradient
  บนสุดแยกสีตามความหมาย (teal/amber/แดง) + hover เงา · panels กระจก · student-row/dept-tab hover
  ลื่น · sidebar gradient teal ใหม่ · **ไม่แตะ HTML structure / module script (loadRiskData คำนวณ
  เสี่ยง มส. จริง) / motion hooks เดิม** — จัดการ specificity ไม่ให้ hover ชนกับ motion system เดิม ·
  ตรวจ id ครบ 7, brace balance OK, ทดสอบด้วย mock (auth-stripped) desktop+mobile console ไม่มี error
- **`app-shell.css` — align สีให้ทุกหน้าวิชาการตรงกับ login/dashboard เป๊ะ (แก้ที่เดียว กระทบ 10 หน้า)**:
  หน้าวิชาการของอีก agent ทำมาดีและ modern อยู่แล้ว (aurora, การ์ดกระจก, header gradient มี eyebrow
  "CRS SCORE / ACADEMIC", ปุ่ม gradient, motion) และใช้ `var(--teal)=#0f6e56` เหมือนกันอยู่แล้ว —
  ไม่รื้อโครง แก้แค่ค่าสีที่ยังไม่ตรง: `--shell-teal-deep #075e5a→#0a5644`, `--shell-teal
  #0b887e→#129a7f`, aurora พื้นหลัง (`rgba(70,190,184)/rgba(111,197,220)/linear 155deg` →
  `rgba(63,182,198)/rgba(15,110,86)/linear 165deg` ตรงกับ dashboard), ปุ่ม gradient `#159586→#129a7f`
  (2 จุด) · bump cache `?v=20260718-7→-8` ทั้ง 10 หน้าวิชาการ (deploy ปัจจุบันยังเป็น -6) ·
  ทดสอบ preview academic/entry.html (mock) ดูดี เข้าชุดกัน
- **ไม่แตะ `app-shell.js`** (งานอีก agent) แต่จะ deploy ไปด้วย (dev ต่าง deploy 35 บรรทัด — งาน
  workflow/motion ที่ยังไม่ push) — verify `node --check` ผ่านก่อน deploy เพราะกระทบทุกหน้าวิชาการ

## 2026-07-18 — แก้บั๊ก iPad: settings card ของ competency-manage แตกเป็น 2 คอลัมน์

- ผู้ใช้ส่งภาพ iPad พบหน้า "กำหนดสมรรถนะ" (competency-manage) ส่วนน้ำหนัก/เกณฑ์แปลผลจัดวางเพี้ยน
  (ตารางน้ำหนักถูกอัดซ้ายครึ่งเดียว note ลอยไปขวา) — หน้าอื่นที่การ์ดแรกเป็นฟอร์มธรรมดา (attendance/
  competency-entry) ดูปกติ
- **สาเหตุ**: `app-shell.js` (decorateWorkspace) ตีตราการ์ดแรกของทุกหน้าเป็น `workspace-primary-card`
  และ `app-shell.css` บังคับให้เป็น **grid 2 คอลัมน์ที่ ≥700px (iPad)** — เหมาะกับฟอร์มกรอง `.field`
  ธรรมดา แต่ competency-manage การ์ดแรกคือ settings (ตารางน้ำหนัก 6 ด้าน + เกณฑ์แปลผล 4 ระดับ +
  โน้ต + ปุ่ม) ที่ตั้งใจเรียงแนวตั้ง เลย 2-col แล้วแตก · reproduce ได้จริงด้วย mock (เติม
  `workspace-primary-card` เอง เพราะชื่อไฟล์ mock ไม่ match `workflows`) ที่ 1024px
- **แก้ที่ `app-shell.css`** (ในบล็อก `@media min-width:700px`): เปลี่ยนกติกา 2-col ให้ลูก**ทุกตัว
  เต็มความกว้างเป็นค่าเริ่มต้น** (`.workspace-primary-card > * { grid-column: 1/-1 }`) แล้ว**เฉพาะ
  `.field` ฟอร์มธรรมดาเท่านั้นที่เข้า 2 คอลัมน์** (`> .field { grid-column: auto }`, ส่วน
  `.field[style*=display:flex]` เต็มความกว้าง) — เนื้อหาซับซ้อน (section/note/ตาราง/ปุ่ม) จึงเต็ม
  ความกว้างเสมอ ไม่ถูกอัดเป็น 2 คอลัมน์
- **ผลพลอยได้**: กันทุกหน้าที่การ์ดแรกมีเนื้อหาไม่ใช่ `.field` ล้วน ไม่ให้เจอปัญหาเดียวกัน
- ทดสอบ (mock + fix, iPad 1024px): competency-manage settings เต็มความกว้างคอลัมน์เดียว ตารางอ่าน
  สบาย ✓ · entry (ฟอร์มธรรมดา) ยังจับคู่ 2 คอลัมน์สวยงามไม่เปลี่ยน ✓ · brace balance app-shell.css OK
- bump cache `?v=20260718-8→-9` ทั้ง 10 หน้าวิชาการ (เพราะแก้ app-shell.css)

## 2026-07-18 — หน้าเช็คชื่อ: ช่องวันที่แคบลง (กันดูกว้างเกิน/ล้นบน iPad)

- ผู้ใช้ขอให้ช่องเลือกวันที่ (`#inp-date`) ในหน้าเช็คชื่อสั้นลงหน่อย เพราะมันยืดเต็มช่องแล้วดู
  กว้างเกิน (แสดงแค่ค่า "18 ก.ค. 2569")
- แก้ inline CSS ของ `academic/attendance.html`: เพิ่ม `#inp-date { max-width: 240px; }` —
  ช่องวันที่จึงกว้างพอดีค่าวันที่+ไอคอนปฏิทิน ชิดซ้ายในคอลัมน์ ไม่ยืดเต็ม (แก้เฉพาะช่องวันที่
  ช่องอื่นคงเดิม) · ทดสอบด้วย mock ที่ iPad 1024px แล้วดูเรียบร้อย
- ไม่แตะ app-shell.css จึงไม่ต้อง bump version — deploy แค่ `attendance.html`
